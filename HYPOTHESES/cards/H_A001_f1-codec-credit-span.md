---
id: H_A001
slug: f1-codec-credit-span
title: (ABSTRACT · unverified) F1 restated — the codec's causal contribution is compressing the credit-assignment span between form and consequence; a refit organ maintains that compression under drift. A falsifiable prediction, no run, no verdict.
domain: write-lever axis (F1) — parked after the H_004 structural terminal
status: abstract
exploration_method: H_004 terminal + campaign re-decision (Fable)
verification_method: none (🜂 ABSTRACT — falsifiable prediction, not yet run)
pre_register_frozen: false
frozen_at: 2026-07-16
deterministic: n/a
llm: none
---

# 🜂 H_A001 — f1-codec-credit-span (ABSTRACT · no run, no verdict)

This is an **imagination/conjecture card** (`H_A*`), tier-separated from the verified `H_0xx`
chain (HYPOTHESES conventions · honesty). It parks F1's restated verbal form after the H_004
structural terminal spent F1 as a *measurable* claim. It is a falsifiable prediction, NOT a
result. Promote to a verified `H_0xx` only if it is ever actually run.

## The restated claim

F1's atomicity thesis is refuted as isolable (`salvage.l11-fusability-is-recoverability`,
`verification-h004-terminal`): for a FIXED morpheme, oracle-fusability IS n-gram-recoverability,
so a static codec's order-≤2 statistics already deliver the receptive value. What verbally
survives:

> The codec's causal contribution is **compressing the credit-assignment span** between a form
> and its consequence. A refit organ's value, if any, is **maintaining that compression under
> drift**. "codec-is-the-self" = the self is what the codec dedicates representation to.

## Why it is parked, not pursued

Every measurable instantiation tried collapses to L11:
- **Receptive flip** — TERMINAL (H_004): frozen order-2 ceiling 0.9954.
- **Generation** — the frequency tautology dual: generating a fixed morpheme = emitting its
  n-gram.
- **Refit under productive drift** — refit is frequency-driven, so it can only grab a form once
  it has fixed, at which point its n-gram is frozen-recoverable; refit and n-gram binding feed on
  the same statistic.
- **Long-range depth** — a model-capacity artifact (raise layers/heads → the reachable order
  climbs → the Δ evaporates); a lever whose existence is a function of the opponent's parameter
  count is a compute-efficiency knob, not "where aliveness lives".

## The one falsifiable prediction that would revive it

Per `l11-design-consequence`, a lever is isolable only if the credit path is forced through it by
a **structural boundary** — information physically absent from the visible context. So the only
non-tautological F1 prediction:

> **If** an experience stream carries a productive (non-fixed) generative process whose parameter
> lives OUTSIDE any practical context window, **then** a codec-refit organ that dedicates capacity
> to that drifting parameter beats a frozen codec on GENERATION of never-seen forms — and the gap
> does NOT shrink as the opponent model scales (distinguishing it from the capacity artifact).

That is a different, expensive rig, and it overlaps the F3 diary's structural-boundary escape. It
is recorded here as the F1 residue, not scheduled — the campaign's belief has moved to F3.

## Verdict

_None — 🜂 ABSTRACT. No run, no verdict. Not part of the verified chain._
