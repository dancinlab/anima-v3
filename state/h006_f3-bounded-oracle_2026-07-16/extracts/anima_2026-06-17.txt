ID collision resolved on rebase: concurrent UsageStore lane merged as H_1391 + G6 as H_1392, so this ko-morphology EMIT-BIAS lane renumbers to H_1393. Smoke cases 120-123→125-128 (UsageStore holds 120-124). Final smoke 123/0. Card/jsonl/FREEZE/result/verdict-dir/probe files/changelog/domain-log/ARCHITECTURE all renumbered; both sibling CORE lane sections kept.
* docs(UNIVERSE+ARCH): H_1427 CA3 replay next-item predictor — card + HYPOTHESES.jsonl + ARCHITECTURE.json lockstep + CHANGELOG + R2 engine-native verdict
feat(self-harness): enforce_anima_gates.py — CLAUDE.md 하드게이트 코드수준 강제 (#2368)
docs(GOVERNANCE): CLAUDE.md 에 배선↔ARCHITECTURE.json 구조 1:1 lockstep 강제 — 배선 done 정의에 구조도 반영, 드리프트=미완 (#2321)
live CORE op wired, ARCHITECTURE.json untouched. TOY fixture, $0 CPU, frozen-first, NO bar moved.
probe reproduces compose 0.760741 BYTE-EXACT; smoke cases 137-140 PASS; ARCHITECTURE.json
🟢 EMIT-BIAS ENGINE-NATIVE BINDING. R1 (next-unit %K) honest miss (on-off=-0.017; %20 alias let blind mode win top-1); a_break_the_wall re-froze on RAW next-unit id with SHUFFLE control KEPT — shuffle STILL collapses → bar1 on-off=+0.0696/shift=0.391, bar2 shuf-off=-0.126, bar3 Ψ-safe. smoke 118/0. Card+jsonl+FREEZE/result+changelog+domain-log+ARCHITECTURE.
research(MITOSIS-ENGINE): H_1410 — G6 IDEATION ★ FALS vs CONV-DEPTH (deep-mouth L1→L4→L8) engine-native .clm decode on aiden pool → 🧱 ARCHITECTURE (conv-FAMILY ceiling), PARTIAL (2/6 per rung, infra-capped) (#2352)
fix(ENGINE+CLM): H_1400 — STREAMING/BOUNDED ConvMoE .clm decode (the H_1392 memory-blowup FIX, byte-exact) + G6 M2-M5 re-score → 🧱 ARCHITECTURE (#2323)
docs(ARCHITECTURE): ARCHITECTURE.json 전수 파일트리 → 구조+노드별 anchor 하이브리드 (전수목록 git ls-files 위임, c4) (#2315)
ARCHITECTURE.json 노드 §주석 미갱신은 done 아님 (드리프트=미완, GREEN-verdict
research(MITOSIS-ENGINE): H_1426 — THE LAST HONEST LENS on thalamus TIMING-axis wall (non-ring + conduction delays, re-open H_1424 defer) → 🧱 TIMING-ARC-TERMINAL across ring AND delay-coupled substrates (mirror-image failure: c4-shuffle now collapses but c2 no robust lift + delay-ablation INERT every seed) (#2354)
wire(ENGINE): H_1414 rung-3/4 — memory×ToM compose op WIRED-live + ARCHITECTURE lockstep (#2340)
엔진배선 ↔ ARCHITECTURE.json 무조건 1:1 lockstep — CORE §섹션·op·slot 배선 후
over the L1 H_1394/H_1403 FALS=0, or whether 🧱 ARCHITECTURE is the conv-FAMILY ceiling.
research(MITOSIS-ENGINE): H_1394 — G6 ★ production-close — 303M-class ENGINE-MOUNTABLE ConvMoE mounted + FALS re-scored (🟢 B1+M1 / 🔴 M2-M5=0 ARCHITECTURE finding) (#2314)
* research(MITOSIS-ENGINE): H_1387 — G6 ★ production-close — 303M-class ENGINE-MOUNTABLE ConvMoE mounted, FALS re-scored 🟢 B1+M1 / 🔴 M2-M5=0 ARCHITECTURE finding
governance(CLAUDE.md): 가설↔배선 규율 강화 — a_verified_must_wire 4칸 사다리 + a_hypothesis_register wired: 상태축 (#2335)
* domain(G6): H_1393 G6 M2-M5 re-score result — 🧱 ARCHITECTURE (FALS=0 over 6/6 GEN=110 C_strong; capacity was NOT the lever)
docs(ARCHITECTURE): re-sync CORE engine wirings — recall_gap·§6.5f emit_compose + full §-section sweep (#2320)
browsable 스냅샷은 on-demand scripts/scratch/gen_file_index.sh → FILE_INDEX.txt(.gitignore).
research(MITOSIS-ENGINE): H_1424 — CONTINUED attack on thalamus TIMING-axis wall (re-open H_1423) → 🧱 MEASURED-CEILING-AT-SCALE (Lens D n=8 + Lens E amplitude-controlled, faithful-IIT4 still can't separate timing from variance every seed) (#2351)
acc_on=0.252 acc_off=0.270 acc_shuf=0.074 shift_on=0.97 — structured head does not beat blind unigram baseline on next-unit acc; investigating root cause (a_break_the_wall, frozen-first)
misled) · c3 EARNED-EDGES FAIL-load-bearing (SHUFFLE=0.983, weight-permute preserves topology →
jsonl: wired=engine-native + engine-native evidence appended to verdict + dfree_other artifact path. smoke 152/0.