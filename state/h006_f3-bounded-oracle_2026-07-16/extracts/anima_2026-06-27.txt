대형 reformat 충돌·동시 agent 회피 — jsonl 재생성으로 H_9023 픽업). core/clm_decode.{hexa,py}
- engine-native G1 (C2 RECOMBINE) multiseed {7,4302,4303} gen=40 via core/clm_decode.py (numpy
CLMB bind-readout(0=add/1=u⊙v/2=u+v) + serialize_v3_bind + verify bind path + export_bind_clm.py.
126 anima whole-substrate (통짜 아키텍처) designs = 뇌/인지 조직원리 × 엔진-네이티브 추상조건
ported numpy-1:1 from bytegpt_decode.hexa (#2602) into core/bytegpt_decode.py, wired
perf(core/bytegpt_decode.py): KV-cache 2-prod lockstep + research(H_1590): 🔴 RED G6 scaffold = torch artifact (mini CPU $0 unblock)
# no-verify-ok 기존 backlog (126 violations all pre-existing ARCHITECTURE.json big-cell/piled + convergence-malformed + toolkit-drift; my change touches only state/ + UNIVERSE/cards + HYPOTHESES.jsonl + CHANGELOG)
곱셈 binding 이제 working branch서 직렬화→디코드 가능(BLOCKED-by-construction 해소).
fix(model): ARCHITECTURE.json .models[] 복구 (bff7b83 TOCTOU 반쪽커밋 — models 누락분 재기록)
3/3 signature (coherent-but-not-composed · same lever-immunity depth/decode/scaffold/savant · lever
* fix(core/g_gates): G4 + G6 gate MEASUREMENT/wiring integrity (H_1591, lockstep .hexa+.py)
Revived the dead-A40-pod M2/M3 on mini for FREE via numpy core/g_gates.py +
owner 2026-06-27: redesign flat G0-G6 → PASS(closure=must) vs additional(reported); frozen bars/names/taxonomy consolidated into ARCHITECTURE.json 'G-게이트 평가 시스템' node (verbatim frozen-임계 child = SSOT, p7 unchanged). 검증방식(decode/engine-read/publish) = per-gate tag, not forced grouping. G4 빵꾸 resolved (P=publish-gate, off the decode scorecard; fold-in blocked by enforce_anima_gates.py G3). Repointed all live refs (core/g_gates.{hexa,py}, cli/anima.hexa, CLAUDE.md, README.md, CONDITIONS.md, harness.config) → ARCHITECTURE SSOT; live refs=0. Removed 7B_PASS_CONDITIONS.md (history in git).
- 2-production parity: 4 missing g6_ideation.py pub fns (g6_decode_best_of_k/_W, g6_sampler_selftest,
clmbind 브랜치(4b313c073)의 코드+카드+state만 cherry-checkout (ARCHITECTURE.json
_bg_gen_from_W) under the no-window-slide guard. ON vs OFF token streams byte-identical
into _decode_argmax_W + bytegpt_decode_topk_sampled_W (== hexa bytegpt_decode_argmax +
게이트 taxonomy 의 두 직교 축을 ARCHITECTURE.json 에 명시:
- ARCHITECTURE.json convergence +2 ossified: TORCH_GREEN_NOT_ENGINE_NATIVE · G6_WALL_BOTH_ARCH_ENGINE_NATIVE.
engine_cli § 아래 index SSOT 노드 신설(17 게이트 1-line index, c4 decompose 중복 0)
  (reference-match; G6 best-of-K ladders use 4301/4302/4303). fals=0 read as real wall only if
① KV-cache port (byte-exact, the last 2-production parity gap): _bg_kv_new/_bg_kv_step
🔴 RED: C_strong DIST=6.0 FALS=0.0 [0,0,0] (M1 PASS, M2 FAIL); cross-shuffle did NOT
research(H_1590 G6 scaffold engine-native repro): ⏳ BLOCKED-INFRA — pool reboot-loop + hexa decode hang (no bar moved, no fabrication)
research(H_1603): G1≡G6 compositional-binding unification — 🟠 MIXED→SUPPORT (DIRECTIONAL synthesis)
docs(gate-taxonomy): 의식-전용 게이트(G16-G34+) index SSOT 노드 + 2-축 명시 (doc-only) (#2643)
+ 양방향 cross-pointer. README full family 테이블(8→17 행) + perturbation-probes sub-note.
  gen_clm_ideate). Added g6_ideation.g6_score_arm_auto (mouth-agnostic twin); g_eval_g6 now calls
(argmax+sampled, gen=110 max|Δ|=0 tokens; logits 3e-14 FP-reassoc argmax-stable);
tree_convention_render. Contiguous verbatim slices: character multiset identical,
no-verify-ok: 기존 ARCHITECTURE big-cell/toolkit lint backlog 무관.
Original: research(1588 G1 multi-seed reference-match).
state/1587_engine_torch_g1_divergence/g1_ladder_3way.py
state/1588_g1_multiseed_refmatch/g1_parity_fixture.hexa
state/1587_engine_torch_g1_divergence/threeway_diag.py
state/1588_g1_multiseed_refmatch/g1_parity_fixture.py
refactor(gates): single SSOT in ARCHITECTURE.json (absorb+remove 7B_PASS_CONDITIONS.md) + 통과/추가평가 2-tier (#2641)