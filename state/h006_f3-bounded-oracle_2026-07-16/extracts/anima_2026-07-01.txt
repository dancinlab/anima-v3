docs(claude): translate live CLAUDE.md to English + wire INJECT-NON-ENGLISH lint (#2665)
Wire lint.injectEnglishOnly = ["CLAUDE.md"] so the sidecar INJECT-NON-ENGLISH gate
into ARCHITECTURE.json convergence.records[] (re-occurrence-prevention SSOT):
Korean is 3 bytes/char in UTF-8 vs 1 for English, and these CLAUDE.md are re-injected
(UNIVERSE 통합카드 대신 ARCHITECTURE convergence에 정리, 4축 exoneration→trunk-objective floor).
cli/train.py가 train/clm/model import, archive 이동=production→archive 위반). 트리 SSOT ARCHITECTURE.json·
Records the ING #33 recurrence-prevention learning in the anima ARCHITECTURE
ARCHITECTURE.json
ARCHITECTURE.json
ARCHITECTURE.json
* docs(ARCHITECTURE): convergence evaluate-hexa-1 갱신 — anima evaluate --py 정식 해결 배선
* docs(UNIVERSE H_9024): ByteGPT recomb-objective verdict 8000-step aiden 재확인 lockstep
train drift: archive/CLAUDE.md gotcha(a) 정정 — train/·training/ repo-root = 정위치(production
(block-severity, staged diff-aware, backtick-exempt) keeps every CLAUDE.md English
InfoNCE recomb-objective A/B(ce_marginal vs infonce, seed7, 8000step, 4/4 held-out DESCENT val_CE 0.70):
* docs(folder-docs): state/·archive/ 로컬 CLAUDE.md 추가 (commons folder-docs)
* docs(arch+archive): train/ drift 정리 + g1 trace 측정정보 ARCHITECTURE 정리
* docs(state/CLAUDE.md): folder-docs 주의사항 — 가설 2표면·RESULT≠가설·slug canonical·pool gotcha
OOM'd hosts when bytegpt_decode_argmax ran on the 303M (crashed summer).
Pin set_deterministic(true) at inference/eval entry points so decode + CE
CLAUDE.md (root) and agent/domains/CHAT/CLAUDE.md.
is an intentional dev-layout dependency (use "quantum_types") and is preserved.
(torch_vs_engine·engine_divergence·decode_strategy) + H_9024 8000step 재확인을 측정정보 SSOT로 보강
archive/CLAUDE.md
fix(bytegpt_decode #21 footgun): bg_load native read_f32_at — no whole-file boxing (byte-identical, host-safe) (#2670)
checkout. GitHub's hashFiles() expression runs with followSymbolicLinks=false
(actions/runner hashFiles.ts), so it stat()s the dangling link, ENOENTs, and
cli/evaluate.hexa (documented in cli/anima.hexa, cli/evaluate.hexa), but the
this is a stale CI reference, not an engine regression.
dangling-symlink + hashFiles followSymbolicLinks=false ENOENT; stale absorbed
fix(bytegpt_decode #21): native read_f32_at weight reader — load RSS 22.67GB->3.24GB (7x, byte-identical) (#2667)
(direct f32->f64 farr, no boxed intermediate): RSS -> 3.24GB peak (7x), acc
* research(H_9024 ByteGPT recomb-objective): 8000-step aiden 재측정 = G1 NOT-SUP CONFIRMED
UNIVERSE/HYPOTHESES.jsonl
UNIVERSE/cards/H_9024_bytegpt_recomb_objective.md
convergence SSOT (cross-repo memory alone isn't visible to anima devs/agents).
Cross-repo memory = project_hexa_anima_linux_build_stale_hexat_setdet.
code-span preserved 1:1; verbatim literal tokens kept only inside backticks):
CLAUDE.md
agent/domains/CHAT/CLAUDE.md
before stat. That file is an external (hexa-lang) source anima doesn't own and
import-closure gate for the first time and failed: module not found
core/g_gates.hexa. That module (G0-G6 scorers) was deliberately absorbed into
Repoint the gate import to cli/evaluate.hexa (preserves scorer-closure compile
within the timeout to save; subsequent warm runs finish in minutes.
job, guaranteeing the job reaches step 11 + Post-Cache save. Pre-existing defect
- cli/CLAUDE.md scoped lockstep · hexa parse OK · py_compile OK
cli/CLAUDE.md
state/CLAUDE.md
bg_load used read_file_bytes(path) = whole 1.2GB f32 binary boxed into a
them); only the whole-file box is eliminated. Byte-identical (max|delta|=0).
docs(convergence): anima-hexa-1 — full-stack build wall = stale pool hexat (diagnose via hexa --version first) (#2671)
CHANGELOG.jsonl
to the coding agent every turn — English cuts per-turn token cost while staying equally
engine-compile job failed on every run at the cache key's hashFiles():
the runner reports "Fail to hash files under directory" — fatal, deterministic.
CHANGELOG.jsonl