cli/anima.hexa anima_evaluate_mode(--py) 경로가 결과파일을 하드코딩 공유경로
fix(evaluate): anima evaluate --py 결과파일 mktemp 유니크화 (동시실행 clobber 근본수정) (#3154)
H2 γ binding-lane 학습→engine-native G1 재측정(frozen bar·통제). 발사=오너 spend-go 대기(rent=spend).
측정 전 no-decode 변동성 사전확인 필수. 확정경로=real-decode op-grip or content-driven 구동.
typecheck PASS. 미배선(en 경로 무변경·ko dispatch는 follow-on). golden-vector+fuzz parity
같은 pool 박스에서 두 `anima evaluate --py` 동시실행 시 서로 산출물을 CLOBBER.
follow-on(별 focused session): hexa 쌍둥이+parity claim·per-cell dispatch·KWR_KO_GATE
fix(cli): anima train/sweep --py 결과파일도 mktemp 유니크화 — 동시실행 clobber 근본수정 (#3154 후속) (#3155)
→ anima evaluate --py --probe 즉시 NameError. infra harness 버그(측정 bar·probe_spec 불변).
convergence(evaluate-py-3): binding 레버 escape 판정 전 handed-vs-blind 통제 필수 (#3138)
  순환성 margin 0.95 · offdiag|cos| 0.035→0.636 = CE 학습이 HRR 준직교성 파괴.
G1 재조합벽=시험한 모든 각도서 terminal 능력천장. + convergence evaluate-py-4(공유 result-file).
convergence(evaluate-py-2): 재조합 벽 측정은 정답이 context에 있으면 copy-confound (#3134)
동일)로 로컬 실행 불가 → daemon full-binary cold-compile QA는 pool follow-on(anima-hexa-2).
동일)로 로컬 실행 불가 → daemon full-binary cold-compile QA는 pool follow-on(anima-hexa-2).
- null L4_clean은 공유 /tmp/anima_evalpy_result.out 오염(L8cov 복사·convergence evaluate-py-4·
L8-cov canonical G1=0 이 측정 artifact(surface-form + T=24 window)인지, matched·
joint-fit이 참 결합세기 비례 held-out interaction-lift + 모든 shuffle 붕괴(THEATER 통제 통과)로
실제 생성 content가 없어 |Δrecon_err|<0.002(불변)→surp_phasic=0.5 상수→shade 0.
H1 atom-cleanness linear-probe(303M pool) ≥0.80 clean→γ정당 / <0.55 blind→terminal.
prereg(H_9235): γ binding-lane on clean atoms — G1 operator 유일 잔여 engine 레버(spend-gated) (#3168)
축자복사=byte-identical). 산술 (b-224)*4096+(b1-128)*64+(b2-128)로 bit-op 없이 py와 수치동일.
Fable C2 설계: 정직플래그 ①최대성공도 canonical G1(추상) 못닫음=형제 G1-concrete scope
Fable C2 설계: 정직플래그 ①최대성공도 canonical G1(추상) 못닫음=형제 G1-concrete scope
(parity-unsafe — 4칸 학습 303M의 en-decode garble 한글이 토큰화돼 frozen kwr 분모 shift).
en 경로 무변경(diff 공집합=frozen bar 구조적 불변) + ko 전용 _rho_fan_words_uni 신설.
stage-2 byte-LM 씬서 정답이 현재 씬에 존재→induction head가 held-out 조합도 복사→
window-resident 재측정으로 판정. anima evaluate --py --probe route 신설(_Mouth 재사용·
결정함수 substrate op는 no-decode 루프서 tick간 불변→degenerate. per-tick 동역학
rf 를 mktemp 프로세스별 유니크 경로 1회 캡처(install.hexa L59 패턴)로 write+cat
동일 경로 보장 + cat 후 rm -f 클린업. non-py/train/sweep 미변경, 표면 불변.
docs(ARCHITECTURE): γ trained-constructive-bind 통합 gate 섹션 + operator-test-py-1 convergence (#3170)
capacity 레버=FALSIFIED. H_1835 메타함정 재발. 모든 학습-encoder 수리경로는 γ로 붕괴.
학습0)가 초과중첩 held-out 짝 복원 M4~32서 VSA 1.0/0.99 ≫ ADD 0.24→0.02 ≫ SHUF 0
fix(H_9209): 계기수정 F1-F3 — POS-FAIL 해소 (Fable 진단, frozen bars 불변) (#3120)
조합-MI를 실제로 나름 → C2 전제 Stage-A 생존(이 캠페인 첫 non-negative 신호). 텍스트가
(ASCII parity·한글토큰·code-switch·이모지구분). 구현됨·미배선(en 경로 아직 미사용).
feat(H_9209): self_phasic emit-shade 측정 arm — #3116 self-fold 벽 돌파 시도 (Fable 설계) (#3118)
안 나름) → 외부탈출도 falsify. 전 각도 terminal. 🟡잔여=grounded/perceptual 짝 re-scope.