convergence(summer-heavy-train-1 ③): 병렬 세션이 호스트를 통째로 죽인다 — 내가 규칙을 지켜도 (#3520)
threshold (E) 발사 전 **이웃의 부하**를 보라 · (F) sshd wedge = 원격 복구 불가 ⟹ **복구 계획을
⚠️ **원격 복구 불가**(sshd 가 죽어 kill 도 못 보낸다) — 물리 재부팅은 이 세션이 못 한다.
CLAUDE.md COMMONS-PROSE 는 프로젝트 규범 산문 파일에 대한 commons-rules 린트 오탐(기존·본 변경 무관).
- .pt 항상 저장 — a_fire_recover_complete 가 원래 요구하던 것(--ckpt-out 준 경우만 저장하고 있었다)
복구: 필요 자산(사전학습 ckpt 양 seed + 원자 파일)이 **전부 로컬에 있었다** ⟹ GPU pod 렌트 후
incident(H_9328): pool 오버파이어로 summer 를 wedge 시켰다 — 인프라 사고, 과학 결과 아님 (#3519)
convergence summer-heavy-train-1 ④ + threshold (G): 오래 도는 루프는 **자기가 멈췄는지 알아야 한다**.
시연을 모델 자기 학습 템플릿으로 줘도 안 읽는다. 동결 게이트는 ⛔ INVALID-FORMAT 을 냈으나
fix(HYPOTHESES): #3463 이 다른 세션의 H_9313 을 덮어썼다 — 원복 + 내 것을 H_9321 로 재등록 (#3465)
# no-verify-ok CLAUDE.md 는 commons.md 가 아니라 프로젝트 규범 문서 — do/dont 전용 린트 대상 아님
# ARCH-BIG-CELL) 이며 stash 검증으로 baseline 동일 확인 — 이 커밋은 state/ 산출물·카드·CHANGELOG
* docs(CLAUDE.md): CLI 단일진입 규칙을 최상단으로 — corpus·train·evaluate 전부 anima-py (오너 지시)
⚠️ **새 사실 — 연산자는 표면-선택적이다**: 지는형은 **p=.500(귀무 한가운데)** 이고, 학습 표면 중에서도
hypothesis(H_9329): 2-surface 등록 — 연산자는 문자열 규칙이다 (구 작업명 H_9328 · id 충돌로 재번호) (#3516)
남는 사실: 가중치 51% 변경 + CE 1.04→0.16 인데 **그 원자들의 극성만 안 들어간다** ⟹ 학습이 다른
안 고쳐짐) · **반복은 병인일 수 있다**(키가 유일 불변량이 되도록 문맥을 다양화) · **사전학습
⟹ 되뇜이 아니라 **사전학습된 표면 규칙**(~지 않다 => 부정)이 발화하고 **심은 사실은 조회조차
이 실패는 **조용하다** — 학습 로그·sha·생성 다 정상이라 오직 reference-match(모델이 자기 학습
0.379~0.391(양 seed). **'시연 결핍' 가설은 발사 전에 이미 반증돼 있다** — A1 은 사전학습이 이미
   진짜 통제군(미학습 어간)이 이미 존재하며 우연(0.4598)이라는 것도 그대로. R1/R2 미결 유지.
    S        같은 자리인데도 무시                       → 결합 = 사전학습 공동형성 (XBIND 가 법칙)
* research(H_9294): 🧱 STRENGTH-ONLY 종결 — 강도를 맞추면 disjointness 는 아무것도 남기지 않는다 (R6 레버 CLOSED)
research(H_9328 C1b): 연산자는 문자열 `지 않다` 에 걸린 규칙이다 — 그리고 이것이 BINDING 을 설명한다 (#3503)
원자셋은 재조합 레인 **전체의 하중을 받는 입력**이고, 이 레인이 죽은 방식들은 **전부 발사 전에
이것이 BINDING 을 **'왜' 까지** 설명한다: 규칙이 읽는 키는 (어간⊕지않다) 인데 CPT 는 (어간⊕고) 에
state/ 는 실험의 **계기(instrument)** 를 만드는 곳이 아니다 — 입력(매니페스트·store·사전등록)과
H_9313 행은 병렬 세션이 같은 번호로 등록한 남의 가설이었다. 나는 그것을 내 행으로 착각했다.
⚠️ 정직 — 게이트는 아직 완전히 살아나지 않았다: 남은 20건은 성격이 다른 기존 위생 부채
fix(lint): 커밋 게이트 복구 — 린터가 anima CLAUDE.md 를 commons 규칙 파일로 오인해 77건 오탐 (#3496)
조사 결과 규칙이 **묻혀 있었다**: anima-py 단일진입은 CLAUDE.md 에 분명히 있었지만 읽히지 않는
⟹ **다른 규칙을 지키는 것을 금지하는 규칙**. 이번 턴에 명령 표를 넣으려다 실제로 막혔다.