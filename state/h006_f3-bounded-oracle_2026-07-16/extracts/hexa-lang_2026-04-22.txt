Design cross-ref: docs/hexa_beta_main_top10_design_20260422.md (B1, B25).
roadmap 57 BLAS-lite). Existing hxblas_sgemm / hxblas_saxpy / hxblas_sdot
docs/roadmap_engine_continuous_meta_proposal_20260422.md after relocating
\$ANIMA/docs/upstream_notes/hexa_lang_beta_main_acceleration_20260422.md
  same_structure  — $HEXA_LANG/.raw-audit SAME_STRUCTURE grep count, w=2,
- socket.hexa         — TCP wrappers around net_listen/accept/read/write/
anima/docs/upstream_notes/hexa_lang_20260422.md §3): tool/auto_tool_index
cumulative via single jq stream over all assistant turns multiplied by a
Brainstorm cross-ref: docs/loss_free_roi_brainstorm_20260422.md (prereqs
feat(tool/hx_raw_coverage): v1 real logic — raw#N rule coverage matrix
panel_emergence() now reads $HEXA_LANG/.raw and renders the top novelty
three smoke properties: (1) non-empty stdout, (2) at least one " ·· "
cert 인프라 · determinism · serve 스택 · matrix primitive 등 29개
Scope: primitive lives in hexa-lang/gate (the language runtime owns shared
feat(stdlib/math): cyclic Jacobi symmetric eigendecomp — roadmap 58 / B3
chore(raw-audit): record 2026-04-21 bypass line from raw_all --fast probe
Keep the lint available for opt-in perf profiling (set HEXA_PERF_WARN=1),
- tool/roadmap_engine.hexa — dispatcher extended with 9 new subcommands
- tool/hx_meta.hexa — dispatcher with 19 subcommands: doctor / health /
feat(tool/hx_coverage_scan): v1 real logic — --selftest coverage matrix
- raw#35 exceptions now document the hook-layer user-approval escalation
http/1.1 server, #04 matrix BLAS-lite, #05 eigen decomp, #06 .meta2-cert
chore(raw-audit): append relock daemon noop event (2026-04-22T12:52:37Z)
              bench-drift, selftest-coverage, api-drift, raw#N-coverage,
                                        3-line ai-native diagnostics on
              hx_selftest_* namespace is write-blocked on this host) ·
Tests 11/11 PASS including 3-run byte-identical on randn_matrix(16,16).
feat(statusline): CONTEXT cache — 60s TTL keyed by cwd+tier, .git/HEAD invalidation
  ./hexa run stdlib/ckpt/test/pcc_tamper.hexa     → 5/5 PASS (all 5 codes distinct)
                       closure_verdict → h_star_verdict → overall_pass →
- raw#13 bans/exceptions rewritten as per-line `key value` (spec array form)
this 3x in one session (evidence: ~/core/anima log 2026-04-22) — info-rich
Shell rc exports WS=$HOME/core + NEXUS=$WS/nexus + HEXA_LANG=$WS/hexa-lang
        "model":{"display_name":"Opus 4.7"}}' | HEXA_LANG=… hexa run …
  transfer        — $HEXA_LANG/.meta2-cert/transfer_*.json count, w=1.5,
gate/claude_statusline_4panel.hexa — new entry implementing the spec in
  🟡 Linux LD_PRELOAD only: raw#21 (Mac SBPL file-link covers hardlink
signal: "🌱 raw#36 ·1h". Awk one-pass extracts the last line matching
panel_emergence(cwd) now covers four axes (spec §18): raw (.raw tail),
cblas headers already pull it transitively, and clangd-lint flagged it.
feat(raw): os-level enforce expansion — CLAUDE.md / per-repo .claude ban, Write/Edit split
feat(stdlib/cert): cert_gate Phase 1 + .meta2-cert schema v2 — roadmap 54 + 59 (B1 + B25)
  export LD_PRELOAD="$WS/hexa-lang/self/native/native_gate.so${LD_PRELOAD:+:$LD_PRELOAD}"
feat(stdlib/ckpt): proof-carrying ckpt format + 5-way tamper verify — roadmap 61 / B5
fix(roadmap_stagnation): decouple selftest from live data via synthetic input (#18)
  · 현재 enforce 중 (7 rule): raw#1/#8/#9/#13/#20/#21/#22 (was: raw#9 only)
  · platform gap (🟡): raw#21 Mac SBPL file-link 은 symlink/hardlink 구분
  Actual enforcement已 landed 2026-04-22 via df2803e1 / 8bc54b0d / 8799d246;
roundtrip, exec_capture split, SOURCE_DATE_EPOCH pin, HEXA_REPRODUCIBLE pin.
  (cross-project catalog moved from nexus to hexa-lang in commit 40b66c44):
Initial sweep: bin_count=2, both shims (no AOT builds yet), 0 stale/orphan.
  · child=active  → parent ∈ {active, done} (siblings OK, still gates
feat(statusline): EMERGENCE panel — latest raw#N + age (S-tier novelty)