  Same class of cross-fn type-inference drift as byte_len(s) drift
gen2_arm_value's existing ExprStmt / ReturnStmt inline path is preserved
각 W-α / W-α' iteration 이 cardinal 을 1 씩 감소 — fixpoint-convergence
의 well-defined progress measure 확보. SH-Ω 이 todo→partial 로 이동.
  W-α / W-α'           — rebuild gates (load <2, clean tree, ≤1 claude)
feat(r4): self/stdlib/argv_skip helper — cross-runtime args() shape unifier
로 분류 → smoke-build-blocking 잔류 cardinal = 2 (aot_build_slot:1761 +
fixpoint-convergence iteration. AOT path 의 clang residual 을 6 category
this file's `Why argv_skip exists` doc block. Single-line addition.
directive 2026-04-27 ("Anthropic API 관련 남아있어 — cli only"). When this
Pure-hexa-native byte-stream → structured-event decoder driven by L1
  that as a reserved keyword ("Scope" token), so it is `vars` here.
    install / CI rebuilds). Each swap shifted whether the cross-fn
Category B (cmd_build): line 1021 functional. W-α' 가 닫는다.
PASS; CLI surface re-checked across the same boundaries — same
  println(1 + 2 * 3)     → '7'    (precedence, parser-driven)
Category A (aot_build_slot, smoke build path): line 1761 만 functional.
            term_winsize_cols / term_read_byte / term_install_sigwinch /
from the /usr/local/bin/hexa wrapper). argv_skip helper only recognized
  - Key (printable ASCII / Ctrl+letter / Alt+letter / UTF-8 codepoint /
terminal-goal `pgrep -f clang | wc -l == 0 during smoke build` 의 첫
self-ref), F (historical/bench): SH-Ω terminal acceptance 와 무관.
Adding `inline` and `restrict` to _hexa_name_is_reserved follows the
  2.G — runtime int→ASCII (divide-by-10 loop in ARM64) so values
removes implicit env reads, enabling in-process selftest of all four
the C compiler ever parses the function — so the formal argument
- gen2_arm_value (line ~4635, MatchExpr branch) has the same drop-
Verified end-to-end via canonical ./hexa build self/tui/input.hexa
emits `HexaVal inline = ...;` and parses as syntax error in clang
- The dict was originally named `scope` but the hexa parser flags
class (1.D run-3 / 2.E / 2.F). No in-scope code path crosses the
are still fn-arg-map views being copied into a local array. The
Tried to dodge the cross-fn map view drop in const_eval_int by
referencing an undeclared `a`. Witness reproduces with /tmp/test_match_inner_let.hexa
Convergence: ../nexus/convergence/r4_cli_only_migration_2026_04_27.convergence
- Plus docs (resolver routing, hexa_real perf, auto_marker, swr pure marker)
  ✓ println(true)                       → 'true'   (2.E witness alive)
  3. L2 input decoder (self/tui/input.hexa) — utf8/csi/sgr-mouse decoder
    statements emit as bare assignments to identifiers that look "already
  self/build_c.hexa        — return-type registry: all term_* → "Int"
    undeclared identifiers. Slot 8ca73cc8297657da (gate/remote_preflight
    Fix: _gen2_arm_is_trivial(body) — true iff body is empty or single
feat(tui-L2): self/tui/input.hexa — input decoder (kick T1.A Layer 2)
    gen2_fn_decl reassigns _gen2_declared_names to the fn's own params
    The same parse-fail shape inline/restrict had (C99) applies to the
feat(self-host): SH4 selftest expansion 3/3 → 7/7 + pure-fn refactor
  daily firings → ~3,200ms × 5,760 ≈ 5 hours/day CPU time freed.
the pure-fn surface + witness coverage now reduces W-α to a one-line
log construction broke with `/tmp/hive-subagent-/usr/local/bin/build/
declare a `let inline` to model parser/codegen state machines around
- Both const_eval_int and const_eval_bool_str now take the vars dict
  4. AST classification         → native if the source is a single
term_poll_stdin + term_read_byte. Closes Layer 2 of the kick witness
  - to_char(b) → chr(b)        (chr is the canonical hexa builtin;
(1) Script-body op-leak — _gen2_declared_names cross-fn boundary.
  activation-cost monotonic decrease counts as fixpoint-convergence
    node["kind"]=="BinOp", even though the same field access works