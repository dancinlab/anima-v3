  work correctly. Silently breaks σ·φ = n·τ master-identity asserts.
downstream consumers (anima Φ pattern, qmirror cond.6) compute a Φ
Downstream R5 verification — staged hexa-bio modules now pass in interp:
Two upstream gaps surfaced during the hexa-chip 2026-05-07 runnable-
Workarounds documented in hexa-chip/UPSTREAM_NOTES.md §U-1, §U-2.
uniform TPM (EI = 0), and cyclic ladder symmetry — 19/19 assertions PASS.
landed 2026-05-06 (RFC-010 cycle) — zero in-tree users + 30+ collision
feat(stdlib): iit_ei — IIT 4.0 conservative Φ lower-bound (EI via KL)
Hypothesis / Proposed fix / Why upstream / Migration / Evidence).
docs(readme): Anima 를 Main projects 최상단으로
canonical-tool-install-paths-shared pattern in working order.
shellout via dispatch.002 child-exec + ---ENTRY--- splitter). Net -226
Per memory feedback "hexa upstream first" — fixed upstream blocker
Adds stdlib/iit_ei.hexa exposing per-state Effective Information computed
future hexa-toml library. The awk patterns scope `entry =` to the
Binary regen separate — main.hexa source change reflected next time
upstream(lexer): pull `partial`/`total` keyword reservation — RFC-010 self-collision fix
Side fixes during regen chain:
as the KL divergence between each TPM row and the column-mean reference,
Loose TOML parse is intentional — full TOML parsing belongs in the
parser only recognized {} and {:.N}, falling through to to_int(spec)
refactor(cache): drop AOT cache — measured slower than cold + ops incidents
_cache_*, list_transitive_imports, cmd_gc{,_impl}, cache_touch_slot,
maybe_aot_gc, aot_gc_audit_log, _aot_layer_root, _aot_hash_*) +
shim (~/.hx/bin/hexa) HEXA_CACHE=0 default dropped — gate gone.
state/hx_tool_graph.json
state/hx_tool_graph.json
state/hx_tool_graph.json
state/hx_tool_graph.json
state/hx_tool_graph.json
After ~/core/hive/.resource and ~/.hive/resource-lb-state migrated to
Files: 5 inliners; 388 LoC removed (raw-format _rstrip + _valid_status +
resource_scorer / workload_router / resource_lint use `--root-key=resources`
(default). load_balancer / lb_state_lint use `--root-key=entries` for
load_balancer also gets writer (_emit_state) updated to emit matching
state/hx_tool_graph.json
ordinary identifiers (test_grokking, test_kan, test_capsule_network,
runtime/arena_pure, hexa_full curry helper, lexer/codegen tokenize, …).
  hexa run build_dispatch → hexa.real (401552 bytes)
  virocapsid/module/cage_assembly_simulation.hexa : sentinel parity vs py
before continuing R5 sunset migration.
self/codegen_c2.hexa
state/hx_tool_graph.json
state/hx_tool_graph.json
state/hx_tool_graph.json
state/hx_tool_graph.json
state/hx_tool_graph.json
state/hx_tool_graph.json
matching the hexa-bio quantum_anima_phi reference impl exactly. Lets
state/hx_tool_graph.json
Resolution order in detect_entry() is now:
  2. hexa.toml [[bin]].path     (first occurrence)
mistakenly match e.g. [scripts].install or unrelated path= keys.
Both follow the existing issues/proposed/ template (Summary / Repro /
format("{:5}", 42) and similar width/align/zero-pad/int-suffix specs
codegen-emitted runtime path (self/runtime.c::hexa_format_n) handle:
trivial path benchmark: cache hit ~603ms vs cold ~275ms (interp wins ~2x
~/.hx/cache/ rm'd (was 8KB, 0 slots). Decision log:
chore(sleep): auto meta pass 2026-05-06T14:55:44Z
YAML on 2026-05-07, the hexa-lang copies were still raw-format-only and
emitted "no entries" against the new SSOT — landing here to keep the
_parse_*_header + _load_* helper bodies), 162 LoC added (yaml_dump.py
tool/resource_scorer.hexa
Pulls the speculative `partial fn` / `total fn` keyword reservation
Use `@partial` / `@total` attribute annotations (parser-level) for
  - lexer.hexa     : 76 sites `total` identifier → `len_chars` (rename
Binaries refreshed via full pipeline (hexa.real + build/hexa_interp.real
re-built but both ignored by .gitignore — they regenerate from the
SSOT in this commit):
  hexa cc              → hexa_v2 rebuilt
  ribozyme/module/bayesian_audit_n30.hexa : __RB_BAYESIAN_AUDIT__ PASS
self/native/hexa_v2