fix(codegen): _hexa_name_is_reserved — add POSIX surface (bind, link, accept...)
- arm64_darwin.hexa : _op_reg → _arm64_op_reg, _op_imm → _arm64_op_imm,
- hexa atlas-audit        → falls through to --help (deferred per above)
  2. Reject when `source = "..."` value literal-prefix-matches "Moal " (single
   grade.breakthrough || edges.breakthroughs || edges.verified_by → violation
but NOT by the interp (grep -rn parse_only self/ empty). That's the root of the
The runtime `hexa_array_sort` (qsort-based) comparator only handles int/float
runtime's own `/tmp/.hexa-runtime/hexa_expanded.*.tmp.hexa` macro-expanded
  pub fn atlas_prefix_linear(prefix) → preserved verbatim (parity test reference)
runtime undefined-function. Combined single-file workaround applied in anima,
  renormalized — status preserved as `status = "open"` continuation field)
`basename.starts_with("hexa")` heuristic to detect interpreter binaries —
   citation_unverified, OR appends falsifier overlay (first-wins semantics)
  - self/codegen_c2.hexa — direct-emit dispatch (1-arg net_set_nonblock,
need no source change. The bucket fast path activates on second call (once
Spec (3 options): default-export-all-fns (recommended) / explicit pub fn /
limit_class_to_string() returns .kind. No sum-type/tagged-union syntax —
- Runtime perf measurement deferred — interp parse dominates wall time;
  p9_server_dispatch_no_walk(state, m, root_qid)  // for static-fid tests
                                exchange-hash H, ed25519 host-sig verify,
fresh ubu-2 clone (`~/core/wilson-deps/hexa-lang/build/hexa_interp.linux`
- doc/atlas/materials_real_limits.md (201 LOC) — verbatim mirror of hexa-matter LIMIT_BREAKTHROUGH.md (188 LOC) + 13-line provenance preface
- compiler/atlas/audit_main.hexa (91 LOC) — CLI dispatcher (external-entity/anchor/all)
  hexa_chacha20_poly1305_encrypt(key, nonce, aad, pt) -> [int]    (ct || 16-byte tag)
multi-MB embed parse 를 module-load 시점에 강제. hexa run (interpreter) 에서
build/stage1/main.c regenerated via self/native/hexa_v2 + tool/build_dispatch.hexa.
    [flat] warn: interp/module_loader not resolvable — falling back to raw src
Completes Phase 5 CLI wiring — `hexa atlas-audit {external-entity|anchor|all}`
  Skipped — those are absorbed in `doc/atlas/materials_real_limits.md` mirror.
  p9_decode(buf)                   — generic dispatch returning a tagged map
- doc/atlas/materials_real_limits.md — LIMIT_BREAKTHROUGH.md verbatim mirror
**Workaround**: explicit wrap via `let wrap_fn = fn(x) { return real_fn(x) }`.
notes: FU2 prefix-bench session — supplemental per-prefix measurement table
wilson's event_bus depends on stability for predictable hook execution order.
firmware/boards/chip/verify/numerics_gpgpu_projection.hexa missing math_pure
1. **hardwall**: grade.value >= 10 && grade.verified ([10*] ceiling) without
checker confirms every recorded source_line still contains the row header in
- Covers drill output + sibling-absorption appends (the most-recently-grown
    forwarding (commits c3a36e96 → 52e12469 → befc5fa2 → 3e9ce3e6 →
- SPEC §5 (D+B post-retire) — interp retirement decision/breakage record
entities + NIST anchor presence) + 2 reference-doc mirrors. Closes the gap
defined identically-named file-local helpers (_op_reg, _op_imm, _op_label,
  `parse_atlas_file` and `overlay_load_cached` patterns. Zero interp risk.
    ProxyCommand / ProxyJump / ConnectTimeout. Matches OpenSSH semantics.
  T1/T2/T3 semantics, sat-1/sat-2, orchestrator pattern, sentinel naming)
  Bekenstein bound / Statistical power / PAC-learning bound (lines 58-63)
| hexa-chip | 42 MB | partial mirror exists; Waves A~M net-new | ~2,980 src + 80 test | partial (firmware/boards/chip/ already mirrors 2026-05-10) |
- compiler/lattice_policy/limit_class.hexa (75 LOC) — enum + 4 constructors + parse/to_string
stdlib/crypto: libsodium-backed primitives (ed25519 + curve25519 + chacha20-poly1305 + sha512)
  hexa_chacha20_poly1305_decrypt(key, nonce, aad, ct) -> [int] | { error: "tag mismatch?" }