이런식으로 처리하면되 AGENTS.tape 기록' + measured session pattern.
cmd_measure 의 exit-90 stub 을 §B 측정 파이프라인 본체로 교체했습니다 —
runtime 의미론은 본 패치 범위 밖 (SPEC §11 v2 borrow checker 의 일).
User goal 2026-05-19: "new paradigm 으로 CUDA 성능·자원·속도 돌파 —
들은 본 패치로는 surface 만 통과 (FAIL 그대로; v2 work item).
- GOAL.md: "현재 정직한 위치" updated from "목표 미도달" to "★ 목표
(string_replace 미intercept — 별개 버그) → epoch→ISO 변환 _iso_utc
- 4 키워드 uniform 표면 `KEYWORD NAME` (no parens). formatter 가 이미
  명시적 no-op 핸들러 (이전엔 unhandled-stmt fallthrough 였음 —
self/std_time.time_format 이 `hexa run` codegen 경로에서 사용 불가
sweep point 는 latency_curve 에서 제외 (거짓 데이터 행 금지).
- self/compiler.hexa — 4 stmt no-op (vestigial bytecode 백엔드, 정합성).
explicitly cites "roadmap 62 통합 후 활성화". phanes measured 4×baseline
docs(PLAN): 진행 로그 — 8 stale-open inbox VERIFIED-CLOSED audit bullet
schema-1.0 F1F2-record JSON 조립 → write_file. `--emit <dir>` 없으면
forge/PLAN 진행 로그. Measurement SSOT: state/forge_rfc049_stage2_2026_05_19/.
g3 honesty — test_ownership_lifecycle.hexa 가 기대하는 move-invalidates
본 패치는 surface only — 키워드가 parse-clean + 실행 시 no-op.
PLAN log: compiler/PLAN.md `## 진행 로그` (g_plan_consolidation single
- PLAN.md: mk2 decision section — full 🍳/🚗 analogy (kitchen modes
- self/codegen_c2.hexa — 4 ownership-stmt 통합 no-op (OptimizeFnStmt 패턴).
종합 성능 벤치마킹 deliverable per the 2026-05-18 directive. Gathers the
docs(README): add 🔥 flame stdlib feature section — hexa-native NN training (2.95× PyTorch eager, measured)
docs(forge): 일단완성 milestone — consolidate forge status to 2026-05-19
campaign table + instrument-first principle + 진행 로그 entry. Scaffold
verify_interp_builtins.hexa was a regression test for self/interpreter.hexa
deploy cycle 에서 binary 자연 promote (본 cycle 미포함, @D g_inbox_
  emits the builtin as a literal forge_dispatch_matmul(...) call (≥5-arg
  - parse-gate 6/6 PASS (lexer · parser · codegen_c2 · formatter · lsp · main)
chore(self): remove verify_interp_builtins.hexa — tests deleted interpreter
Filing-only (no SSOT change), per @D g7 / g_inbox_processing_loop FILE step:
Stage 1 9.67x anchor) — all 3 falsifiers PASS (WIRED-PERF / WIRED-CORRECT
별건 #1 잔여 — test_keyword_audit.hexa L172-189 의 4 ownership-group
cycle-accurate parity 가 아니며 scope_caveats 4건에 명시. saturated
  F-RFC060-VERIFIED-CHAIN  — KILL→downgrade ($0 paper): rmsnorm kernel
  PARADIGM_C_RESEARCH.md (new) — 8-paradigm survey (dataflow · CGRA ·
REGRESSION FIX. The final R7-close parity run regressed (atlas 118→112,
already-measured GPU-dispatch walls (no new fires) into one SSOT section:
in distributed shared memory — vs the RFC 049 cuBLAS GemmEx BF16 chain.
definition in the current tree: codegen_c2.hexa emits hexa_farr_rope_gpu,
fix(codegen): skip @-attribute declarative AST kinds — VerifyStmt/IntentStmt/GenerateFnStmt/GenerateStmt/DeriveDecl
  channel  — construct unused; future concurrency channels = 'chan' (Go convention)
- self/parser.hexa — 헤더 AST-kind 표 + parse_stmt dispatch (Own·Borrow·
zero behavior change for CPU codegen (F-RFC055-CPU-CODEGEN-UNTOUCHED holds).
  - atlas_verify_smoke 117/118 (5 FALSIFY → 1; s8_elliptic_j1728 residue)
  Phase 1 substrate      — verified (RFC 040 cuBLAS 4×, RFC 041 kernels)
declaration-flavored (compile-time-only) — the AST node exists for future
with no Sigv4Header/Sigv4Request constructor defs → ~10 clang undeclared-
interp-direct-invoke branch — `bin run <file>` is compile-then-exec now,
storage class — the production runtime_bf16.c body, not the bare Stage 1
type-level construct with no runtime code, like a trait/interface decl —