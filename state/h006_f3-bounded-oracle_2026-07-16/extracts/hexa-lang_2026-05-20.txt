Tier-A.3 still OPEN: _fopen · _fclose · _fread · _fwrite · _fseek · _ftell · _fdopen · _flock · _setvbuf (file-stream subset; needs FILE* abstraction layer).
step-1 (C-source scaffold) of the hexa-native runtime rewrite. Adds 3 `static __attribute__((noinline)) hxlcl_strlen/memcmp/strcmp` helpers in self/runtime.c above the runtime_core.c include, plus textual `#define strlen hxlcl_strlen` (et al) that redirects every subsequent call — including macro expansions and inline header bodies that clang's libcall recognition under -Oz would otherwise re-pull to libc.
Closed: _fopen · _fclose · _fread · _fwrite · _fseek · _ftell · _fdopen · _flock · _setvbuf.
cycle 47 = batch follow-up to cycle 46 (#241 merged `0bece833`). Same `hxlcl_* + #define` method extended to `_strcat` (+ residual `_strlen` closed in same surgery) · `_strchr` · `_strstr` · `_strndup`. Adds 6 helpers (`hxlcl_strcat/strchr/strrchr/strstr/strncmp/strdup/strndup`) and corresponding `#define` redirects in self/runtime.c.
harvest(rate-limited): D5 partial — G5 fs follow-on (stat/rotate/mkdir + test_fs_atomic) parse-gate PASS
Closed (8 stdio + 1 recovered strlen): _printf · _fprintf · _snprintf · _fputs · _fputc · _fflush · _putchar · _perror · _strlen.
- numeric batch: `_atoi` + `_atoll` + `_atof` + `_strtoll` + `_strtoull` (5 helpers + #defines + perl substitution)
residual (3 stubborn): `_strdup` / `_strncmp` / `_strrchr` — clang -Oz reverse-libcall recognition converts `hxlcl_*` patterns back to libc-shaped calls (e.g., `hxlcl_memcmp(a, b, constant_k)` → `_strncmp`; `malloc(n+1) + byte copy` → `_strdup`). `-fno-builtin-NAME` tested and ineffective on -Oz; flag removed. Defer to cycle that rewrites the helpers (`@asm` block, explicit side-effects, or step-2 hexa-source port).
cycle 49 closes Tier-A.1 final stragglers (`_bzero` + `_strncpy` + `_strcpy` + `_strerror` + `_strftime`) and drops 3 of 8 Tier-A.2 memory symbols (`_memset` + `_memmove` + bonus `___memcpy_chk`). Adds 7 new `hxlcl_*` helpers (memcpy/memset/memmove/strncpy/strcpy/strerror[stub]/strftime[stub]) and corresponding `#define` block redirects.
residual: `_strlen` 1 stubborn libc call chained from `_strcat` inline path (disasm `bl _strcat` → `bl _strlen` at 0x100000820). cycle 47 closes both in the same surgery (`hxlcl_strcat` + #define strcat).
cross-link: RUNTIME.md ## Log cycle 50 + Tier-A.6 [partial] · compiler/PLAN.md 2026-05-20 cycle 50 entry · cycle 49 commit `8186bcfc`/`0121f98a` rebased.
docs(rfc-070): G7-A.native impl.falsify RETRY PROBE — DEFERRED on worktree HEAD (heritage cascade absent on both origin/main and worktree branch)
docs(governance): commit-push must carry the deployed binary (@D g_commit_push_deploy)
cycle 52 = Tier-A.3 stdio printf-family minimal implementation. Adds `hxlcl_vsnprintf` (~90 LoC handwritten format scanner) + wrappers: printf, fprintf, snprintf, sprintf, fputs, fputc, fflush, putchar, perror. Format scanner handles %s/%d/%i/%u/%lld/%ld/%llu/%lu/%zu/%c/%x/%X/%p/%%, basic width + zero-pad + left-align. Float specifiers emit "(float)" placeholder — compiler hot paths don't print floats. `-fno-builtin-strlen` added to build_aprime.sh (was ineffective in cycle 47 but works here because the new code triggers a different optimization pass).
cycle 54 = Tier-A.3 file-stream subset port. Adds 9 helpers (fopen/fclose/fread/fwrite/fseek/ftell/fdopen/flock/setvbuf) + #defines. FILE* encoded as `(void *)(uintptr_t)(fd + 1)` so 0 doesn't alias NULL. _hxlcl_fp_fd helper: small values (<0x1000) = our encoding; large values = libc FILE* (pointer compare against stderr/stdout/stdin). fopen wraps `open` syscall; fread/fwrite call `read`/`write`; fseek/ftell call `lseek`. flock + setvbuf = noop stubs.
Tier-A.1 = CLOSED ✓ (15 of 12+ symbols dropped). Tier-A.2 = 3 of 8 (memset/memmove/___memcpy_chk closed · memcpy residual · malloc/free/realloc/calloc/mmap/munmap still open — these need hexa-native bump allocator + mmap syscall shim cycle).