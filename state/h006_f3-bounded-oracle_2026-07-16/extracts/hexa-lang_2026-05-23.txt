    let 변수 = 1      // Korean — Parse error: expected identifier, got Eq
다음 cycle 이 동일 audit 을 재실행하지 않도록 결과 + 적용 원칙 +
fix: unbreak bootstrap CI — flatten use-closure + arm64-Linux syscall gate + install.hexa shim (#341)
지원한다 (self/lexer.hexa:396-441). 본 RFC 는 기존 부분 구현의 두 gap 을 다룬다.
feat(lexer): warn on JS-style '${...}' template syntax in string literals (PROBE r14 A) (#478)
첫 rename PR #387 (`codegen_c2.hexa` → `codegen.hexa`) 외 후속 후보는
  - PASS threshold: ≤ 0.05  →  PASS (58% mean-error reduction · honest)
feat(error): parse error render — source snippet + caret pointer (PROBE r10-18d) (#444)
regen(hexa_v2): activate cycle-3 batch (unicode idents · trailing comma · exec3) (#454)
expression kind: IfLetExpr` 에러로 실사용 불가. `while let` 은 parser
Fix: after ret_type parsing in parse_fn_decl and before parse_block, peek for
구현(parser.hexa L2535, `IfLetExpr` AST). probe 측정: codegen `unhandled
extern. (The cycle-66 read/write/close/dup2/pipe/fork/waitpid extern decls
  identical pattern already present in the string branch (lines 3303-3318)
- Rounds 7-8: posix-fs builtins · write_file CRITICAL · runtime.h mass-decl · clang fork-storm · exec_with_status3 (PR #395, #400, #407, #410, #414, #441)
- Round 10: @derive · attrs · UTF-8 idents · caret render · trailing comma · pub-use (PR #435, #436, #440, #443, #448, #449, #452)
fix(runtime): stat/fstat/lseek/mmap libc-wrappers — Darwin arm64 syscall class (PR #426 follow-up) (#431)
* chore(interp-retire): dead-script audit — drop obsolete parser-SSOT lint, repoint flame builds (#340)
(impl 사이클 진입 가능). INDEX.md Summary 표도 D1 결정 반영. HEXA-LANG.log.md
`@D naming_generic` (#384) governance landing 직후의 전수조사 결과 기록.
refactor+feat: codegen identifier rename + re-land 4 conflict-blocked PRs (#403)
- SDK invariants (lifecycle hygiene, single-source SSH key, idempotent restart)
mixed-pattern modules (cycle 2/I telemetry_status.hexa hit it during render).
4 PR stack 제안 (~120줄): III-1 spec 화 · III-2 r-prefix · III-3 stdlib
  pub struct P { pub x: i64, y: i64 }    → parse error at the field `pub`
- 기본 `"""..."""` multi-line OK (단일/다중/JSON/empty 모두 정상)
single file. Verified end-to-end on macOS arm64: flatten (22 files, 21944
- this PR global insertion: between L60 `fn tc_error_at` and L62 `// Scope` separator → non-adjacent
feat(wind/kernel+producer): power_curve_segments v0.2.0 · Vestas V90 PASS · κ-71 G41 D122 (#325)
fix(runtime.h): mass-declare 102 codegen-called fns — close decl-debt backlog (#360)
- 새 섹션 "architect decision" 추가 — RFC 081/082/083 각 D1-Dn 결정 대기
Implementation 전 단계 — 결정 포인트 (Dn) enumerate + 권고 명시.
regen(hexa_v2): activate closure-by-ref + sha1 codegen (cycle-1 batch) (#437)
Re-ran the round-10 P0 repro (≈200-char identifier, decl + use) against the
미지원(`Let` unexpected). 5-PR stack 제안: codegen IfLetExpr 분기 →
  - `parse_struct_decl` calls `parse_visibility()` before each field name.
fix(parser): attr whitelist + conflict warnings (PROBE r10-15b/c/d) (#448)
- Rounds 4-6: match-arm guard · OR/Range pattern · format brace · iterator aliases · bool coercion · closure mut-capture (PR #379, #380, #381, #385, #393, #433)
* feat(stdlib/kernels/wind): power_curve_kernel v0.2.0 — power_curve_segments piecewise-linear API (κ-71 G41 D122 kernel refinement)
docs(inbox): naming-generic audit 2026-05-23 — 1 rename + 8 legitimate + 3 time-gated (#391)
fix(runtime): array index error messages — preserve original idx · diagnose string (#355)
feat(stdlib/plot): minimal native SVG charting — close paper-figure plotting gap (#430)
    rfc_083_tls_primitive.md            (200L · D1-D7 · 권고: system 동적 링크 D)
fix(self/main): content-addressed hexa run/batch cache — kill clang-fork storm (#359)
operator), RFC 083 D2 (sync/async API) — 또는 D1 결정만으로 진입 가능한