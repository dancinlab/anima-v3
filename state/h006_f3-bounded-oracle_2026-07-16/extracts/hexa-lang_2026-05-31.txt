docs(unshadow): knownint-rawlocal — add §knownint-rawlocal bench + log entry (CLOSED-NEGATIVE reconcile, follow-up to #2226) (#2227)
box into a register (.tag dead, .i promoted) — the inline .i slice already emits
local recovers the f_inline→d_ideal Δ and reaches the register-pack ceiling.
fix(cloud/dft): canonical scf.in split — parse sections, emit namelists-first (#2278)
  the i64 LANDED (1.18x), because the inline .f read is a single-field {.tag,.f}
feat(hexa-cc-zero): C-front prep — native_gate build harness + emitter-header BUILD doc (deletion 🟠 DEFERRED on Linux smoke) (#2220)
  LANDED (1.18x), because the inline .f read is a single-field {.tag,.f} compound
PR #2233 merged the escape->stack non-flat docs but a cherry-pick mishap dropped
PR #2233 merged the escape->stack non-flat docs but a cherry-pick mishap dropped
WIPE-OK: native_gate.c (1488 lines) superseded by native_gate_emit.hexa byte-identical SSOT — proven #2218, re-confirmed post-deletion; smoke demoted to optional per user gate redefinition
fix(cloud/dft-run): cost cap on rent guard — stop renting $4-12/hr B200 GPUs for CPU-bound DFT (#2225)
test(hexa-cc-native): N4 — clang-free native exec chain falsifier (0 C, 0 fork → exit 42) (#2252)
Revert "fix(hexa-cc-zero): P7 edge-miss frozen-seed fallback for runtime.a + hexat (#2228)" (#2229)
fix(cloud): thread -i identity into vast SSH opts + never teardown on unreachable --resume (#2280)
feat(codegen/unshadow): non-flat array descriptor stack-alloc (escape->stack sub-task) (#2233)
feat(hexa-cc-zero): C-front — native_gate.c emit+byte_diff pair (gate-1 PROVEN) (#2218)
→ fixpoint → cross-arch → CI gate). Docs/planning only — no code/build change.
* docs(unshadow): real measured numbers for escape->stack non-flat verdict/bench/log
* docs(unshadow): real measured numbers for escape->stack non-flat verdict/bench/log
User decision (2026-05-31): for this work session, do NOT ask at 4-axis forks —
docs(buildfloor): correct M1/M2 verdict to 🟢 — smoke result verified (#2277)
User decision (2026-05-31): always advance HEXA-CC-NATIVE along the ①완성도
(import edit busts the key · no-op recompute is stable · no-import entrypoint
feat(hexa-cc-zero): C-front CLOSED — git rm native_gate.c (byte-identity SSOT · self/native .c → 0) (#2222)
fix(cloud): scp keepalive (ServerAlive 15x4) — stop orphaning rented pods on a mid-transfer stall (#2230)
- sequential traverse+sum 2.5–2.8× 느림 🔴 · value-pass 30–36% 느림 🔴 (register-fit 반증)
- sequential traverse+sum 2.5–2.8× 느림 🔴 · value-pass 30–36% 느림 🔴 (register-fit 반증)
docs(gpu-roofline): 1b 체크박스 [ ]→[~] SSOT 정합 — frontend N-capture LANDED (#2256) (#2257)
docs(gpu-roofline): 1b 정직성 정정 — fabricated "LANDED" 평결 철회 + honest PARTIAL (#2259)
docs(runtime): 마일스톤 A verdict fabrication retract → 정직 PARTIAL (PR #2266 정정) (#2268)
docs(hexa-cc-native): @policy +SESSION-AUTO — auto-pick completeness axis this session (#2267)
fix(cloud/dft-run): normalize deck outdir/pseudo_dir to ./out · ./pseudo before launch (#2219)
  UNVERIFIED · LANDED 철회)". REAL=parser dc49c0048 착지+cc--regen rc=0 · UNVERIFIED=
fix(codegen/unshadow): land non-flat array stack-alloc codegen dropped by PR #2233 (#2237)
fix(hexa-cc-zero): P7 edge-miss frozen-seed fallback for runtime.a + hexat (#2228)
flip to [x]. Static validation passed (brace/paren balance, all F64 fns + runtime
(arm64) exporting two defined external __text symbols — _hexa_exit (HexaVal-ABI
Ruled-out axis: source-level box-stripping of a single-int accumulator is a dead
1. The native backend canNOT emit the whole compiler: flatten compiler/main.hexa
   keeps importing gen.hexa — it is the top, not in the cycle). Cycle broken.
  HEXA_NATIVE_ARR flag (escape hatch). Escaping bindings keep the boxed HexaArr
docs(unshadow): knownint-rawlocal — milestone flip [x] 🔴 + §knownint-rawlocal bench + log (CLOSED-NEGATIVE reconcile) (#2226)