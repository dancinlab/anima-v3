⚖ 비교 — g5: dft_dispatch_test 확장 — JSON well-formed·전 단계 present·빌더 byte-identical
* feat(hf): validate.hexa — HF-artifact validation harness (pull → on-core RUN → verbatim verdict)
- cloud_cli.hexa: vast/runpod 양쪽 --force-new 분기에 가드 배선 — 캡 도달 시
feat(hf): validate.hexa — HF-artifact validation harness (pull → on-core RUN → verbatim verdict) (#2484)
* feat(qforge): CaH6 composition cross-val — real composed lambda VERBATIM (PARTIAL, screened [d] BLOCKED)
d6 HONESTY: 포논 합 broaden 은 진척이나 1% 닫음 아님 — 남은 3 breadth
runs cuBLAS Dgemm and on a no-CUDA host falls back to the byte-identical RFC 032 CPU
feat(cloud): hexa cloud alive <id...> — provider-API liveness (unreachable-vs-dead disambiguator) (#2498)
feat(selfhost): byte-eq graduation — hex-literal fix + verdict + open post-grad domain gates (#2532)
the CPU even on a CUDA host. Both fwd+bwd ride this one dispatch, so both were host-bound.
  4.376 NOT forced; the small real composed λ + screened-BLOCKED are reported as-found.
* feat(cloud): hexa cloud api <provider> <METHOD> <endpoint> — universal authenticated REST passthrough
Records verification outcome on top of #2472: conv fwd+bwd byte-eq ALL-PASS (max|Δ|<=1.1e-16,
the Gamma-only N(E_F) under-count had MASKED a coupling over-count; fixing the DOS exposes it.
keeps the Picard iteration operator (1−β)I+βL ≥ 1 for all β. The screened fixed point
  분산(단일 Einstein 아님)+오프대각 inter-band |g_mn|+실 N(E_F)+k-mesh BZ합.
  깨끗이 스킵. 기존 dispatch/.dft_stage/체인은 byte-identical (회귀 핀).
feat(qforge): metallic-wall breadth COMPOSITION — all-4-bricks on one converged CaH6 cell (PARTIAL, screened [d] BLOCKED, verbatim) (#2500)
* feat(qforge-nc): fcc-Pb composed el-ph validation — λ=1.546 vs lit 1.4-1.6, STRONGEST-coupling rung, FREE native (no QE) (#2513)
feat(qforge): variational Kohn-Sham total energy — scf_etot (edc double-count + Ewald) + qforge_scf_smeared_dc (#2530)
feat(qforge): generic Anderson affine fixed-point solver + g5 (I−L)x=b linear-algebra identity (#2520)
비유: 한 층씩만 보던 건물 진동에 층간 결합을 더하니 흔들림 ~3배로 넓어짐.
  (ii) 명시적 re-arm 후 재개 허용, (iii) 동일 (project,owner) (N+1)번째 급속 force-new
로 작은데, 이유는 el-ph 포논 합이 너무 NARROW — fixture 가 단일 하드코딩
  HF.jsonl row schema + dancinlab CLM 컬렉션 plan. util-GREEN 착지 시 PUBLIC 업로드 =
Einstein ω₀=1236.4 K 하나에 모든 가중을 몰아넣기 때문. 브릭 (a)는 그걸
domain(FORGE-UTILGREEN): register flame+forge GPU util-GREEN endgame domain to main (#2523)
ts·stage·event·wall_s·rss_kb·exit)을 INGEST하는 순수 분석기를 추가한다.
gen2 self-host byte-eq fixpoint achieved (stage3==stage4 BYTE-EQ-GRADUATION, gen4.done).
- 단일 라인 픽스처(+concat 제거) · _ck 불리언 묶음 단언으로 통합:
  ssh_host+ssh_port 를 PodEndpoint 로 반환(순수 read, mutate 없음). 없거나
* test(cloud): g5 selftest for `alive` — mocked vast+runpod JSON fixtures (RUNNING/STOPPED/GONE/UNKNOWN/MISSING-CRED)
* fix(cloud): alive — downgrade GONE to UNKNOWN when the vast instance-enumeration call failed (d6 no false GONE)
* flame(clm): host $0-CPU 재export 드라이버 clm_reexport.hexa — v0.2 .clm REAL 학습 (forge 0·torch 0)
비유: 빌딩 층끼리의 진동을 각 층 따로(대각)만 보다, 층간 흔들림 결합까지 본 것.
feat(qforge-nc): bcc-Nb composed el-ph validation — λ=1.190 vs lit 1.0-1.3 (STRONG-coupling rung) (#2512)
  self/runtime.c+cuda 시그니처 = pod 빌드 필요 → DESIGN DOC only. util≥20% 는 fire verdict.
fix(bench_runner): headless-safe hexa launcher + repo-root — drop foreign-host fallback (#2471)
test(qforge): g5 gate for Allen-Dynes f1·f2 strong-coupling factors (CaH6 critical path) (#2517)
pod vast 39082940 · a_lane_akida_gpu_split — AKIDA 무병합). 직전 드라이버는 closure