* feat(qforge): real-CaH6 UNIFIED off-diag xval + VERDICT — architecture CLOSED, magnitude residual (d6/@L5) (#3059)
OP-43 re-applies OP-41's per-builtin g5 method (AOT probe + arg-shape-trap control +
    benchmark_all): baseline exit_code=-1, never-passing → NOTE pointer comment.
domain(hexa-0pod OP-44): runtime float formatter non-correctly-rounded — MEASURED + libc-delegation fix PROVEN → 🟠 DEFERRED (frozen-seed re-pin) (#3090)
codegen.hexa → hexa_cc.c → clang → fixed hexat. Emitted C now `hexa_float(-0.254829592)`;
the freshest local native compiler (~/.hx/bin/build/hexat, Jun-8) → it is a REAL bug in
* domain(hexa-0pod OP-44): runtime float formatter hxlcl_vsnprintf non-correctly-rounded — MEASURED + libc-delegation fix PROVEN → 🟠 DEFERRED (frozen-seed re-pin)
feat(hexa-0pod OP-42): OP-40 hex-float fold-serialize → determinism contract + OP-39 gate +5 hex-float cases (#3091)
DEREGISTERED 16 (0-shadow + example-only baseline-dead callers, weight_dict 0-call orphan):
    DEFINES a local 4-arg fn adam_step → resolve LOCALLY, not the builtin → NOTE.
No live surface references the builtin → deregistered from the self/env.hexa roster
domain(HEXA-0POD OP-39b): seed-promote+gate-flip → 🟠 DEFERRED (frozen-anchor re-pin, out of 0-pod scope); fix PROVEN correct + fixpoint-stable locally (#3083)
* feat(hexa-0pod OP-42): OP-40 hex-float fold-serialize → determinism contract + OP-39 gate +5 hex-float cases (13→18)
feat(qforge): UNIFIED dense off-diag V_scr(G_a−G_b) el-ph vertex assembler (L1/2) (#3058)
SURVIVE-WITH-REASON 26 (local-fn shadows in real programs and/or large broken self//stdlib
threshold (‖L‖→1), giving ‖ΔV_scr‖/‖ΔV_bare‖=3.5e7, conv=false (#2786).
threshold (‖L‖→1), giving ‖ΔV_scr‖/‖ΔV_bare‖=3.5e7, conv=false (#2786).
DECISIVE SURVEY FINDING: "the CI seed" is an IMMUTABLE FROZEN GIT ANCHOR, not a
The bare Hartree kernel 4π/|G|² diverges as |G|→0; on the converged CaH6
The bare Hartree kernel 4π/|G|² diverges as |G|→0; on the converged CaH6
* test(qforge): g5 gate for TF-screened Coulomb — k_TF² derivation + |G|→0 bound + gain-bound A/B
local-fn-shadow vs builtin distinction + dead/live caller classification) to the 42-member
inline lbo×sbo×boff×swmode sweep — NO config reaches 0 → D1 FALSIFIED (canonical
FIX PROVEN: route the float branch to libc snprintf (OP-37b atof→strtod pattern
deterministic). Same sweep delegating to libc → 0/5521 round-trip fail, 0/5521
feat(hexa-0pod OP-35): checkpoint save/restore determinism (6th surface) — FCK\x01 fp64-LE training checkpoint, resume==uninterrupted bit-eq, cross-platform byte-identical (#3062)
fix(hexa-0pod OP-40): comptime float-fold 1-ULP residual — ROOT-CAUSED to host %.17e serialize (not the multiply), FIXED bit-exact via hex-float → MAX 0 ULP (#3084)
* domain(HEXA-0POD OP-40): verdict + milestone [x] — comptime float-fold 1-ULP ROOT-CAUSED to host %.17e serialize, FIXED bit-exact (hex-float) → MAX 0 ULP
* docs(hexa-0pod): OP-41 verdict + milestone [x] + log — complete 231-builtin falsified matrix; optimizer-scheduler family closed
test(qforge): g5 gate for TF-screened Coulomb — k_TF² + |G|→0 bound + gain-bound A/B [stack 2/2] (#3039)
local single-PR task — the task's explicit "regen carries unrelated drift → DEFER".
    parse/re-serialize) → BYTE-EXACT for the dominant 0.0-X idiom (A&S erf coeffs).
kernel is finite & bounded by 4π/k_TF² as |G|→0 where bare 4π/|G|² diverges.
@L5: the functional axis (LDA→ALDA→PBE/GGA, SCF + χ-kernel) is exhausted. The
3.3e-7), so the migration-gate ARCHITECTURAL residual is CLOSED. But unifying the
* docs(qforge): PBE-SCF functional-alignment lever = @L5 CLOSED-NEGATIVE verdict
Self-host fixpoint GREEN: fixed hexat recompiling fixed SSOT -> gen-N==gen-N+1 C
docs(qforge): PBE-SCF functional-alignment lever = @L5 CLOSED-NEGATIVE (#3040)
* feat(qforge): UNIFIED dense off-diag V_scr(G_a−G_b) el-ph vertex assembler
=> full USPP/PAW Route A CLOSED-NEGATIVE, matches arXiv:2507.06749. ALL paths