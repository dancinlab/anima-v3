(progress lives in the domain, not scattered). All are genuine large/blocked
fix(build): linux x86_64 toolchain — drop Darwin-only -arch flag + harden runtime.c gen (build_aprime.sh · install.sh) (#3317)
  gelu · gelu_bwd · ce_grad · int4_quant · int4_quant_bwd · groupnorm_bwd ·
fix(headless-foreign-path): migrate /Users/ghost literals to env→cwd-ascent (self/attrs · embed_gen · hw_probes · hxc_a25) (#3318)
pod CPU-only QE · import-resolver argv0 ordering · pool-host stale artifacts ·
Repro: stdlib/bio/nanobot/module/actuation_simulation.hexa (defines isfinite).
· tool/regen_{remaining8_foundation_c,vendor_native_c,dispatch_c_artifacts,
  (drives each new host dispatch + compares to an inline unfused reference).
lora_cuda_host,hxqwen32b,hxqwen14b}_byte_diff.hexa · self/native_gen_byte_diff.hexa
deck-gen non-ASCII QE parse · build-cache transitive imports · isfinite codegen
collision · dft-run unconditional recover · GPU OpenMM/FEP provisioning · vast
Covers: cloud --resume fildyn normalize · idle-reap provider-status · vast GPU
  emit rc=0 · MAX 3 procs (no 1800+ storm) · 334332 B runtime_cuda.c (>>100KB,
  pre-existing raw_exec op already runs arbitrary unsandboxed commands with no
The C-backend already mangles reserved C names to `u_<name>` via the shared
Change is comment-only: documents the dispatch contract in the emit SSOT so
module while the entry file is byte-unchanged served a STALE cached binary
Three legit untracked files were sitting uncommitted in the worktree:
docs(forge/7010d588-f1): falsify sm_120 tier-dispatch arch-gate hypothesis — no allowlist exists; routing is HEXA_CUDA-gated, cuBLAS Dgemm verified on-device on RTX 5070 sm_120 (#3336)
Source + self-host compiler proven correct (native gen3 -> 9, fresh hexat -> 9; parser
guarded #ifndef HEXA_CUDA (GPU build UNCHANGED), define both bare + hexa_ names,
block in `_hexa_name_is_reserved` simply lacked the four classification macros.
programs keep the base-only key; commented `// use` lines are not mis-parsed.
are equally-cheap boxed calls). Algorithm already optimal (K is module-level,
PR #3317 follow-up: keystone verify on summer (linux x86_64) found a SECOND
generation behind source forever (the live edge build/hexat from HEAD still
generation behind source forever (the live edge build/hexat from HEAD still
  self/ + stdlib/ = exit 1. Porting it = re-introducing dead code (no call
back to the entrypoint-only hash. Result: editing a transitively-imported
findings(d8 runtime/da19aa72): hot-loop array leak root-caused to non-reclaiming bump allocator + escape-barrier gap — NO corrupting fix shipped (#3332)
finding(sha256/0b23189b): pure-hexa sha256 56× gap = HexaVal-boxing codegen WALL (case a) — route forge to runtime builtin (C/OpenSSL parity) (#3334)
chore(domains): absorb 4 GPU/feature-blocked handoffs into FORGE-UTILGREEN + QFORGE-PERF SSOT (#3335)
feat(cloud/fb0ae653): add `--json` to `cloud resolve`/`ssh-port` — stable {"host","port"} (#3329)
release-pipeline dead-end. Ground-truth section (gen3 --emit → 9) was correct and stands.
pattern: env var → ascend from cwd to the repo sentinel (self/module_loader.hexa)
descriptive rc=-1 #ifndef HEXA_CUDA stubs (the exact contract codegen documents)
(no `[float]` intermediates escape), with one reusable per-axis line scratch.
The default human output is UNCHANGED — `--json` only swaps the format when
  ~2.5e-7 GELU divergence (NOT an OP-37 regression). Held back to keep OP-37
such table exists: there is no sm_xx allowlist anywhere in the dispatcher or
Re-checked against current origin/main (f1832e1) on linux pool host summer:
- grep: ZERO functional /Users/ghost literals (only doc-comments explaining
3rd Li2MgH16 attempt (instance 38917745) with --query direct_port_count>=2
  rules_loader repo-root defaults + publish/_publish_registry domains.json
native emitter accepts; LLVM aarch64-/-unknown- spellings are rejected).
Same bug class as the -arch leak. Now set DEAD_STRIP in the uname block:
and finally run unguarded where even that is disallowed (graceful, g1).