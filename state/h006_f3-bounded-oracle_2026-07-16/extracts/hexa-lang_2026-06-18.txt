install.sh clang-18/24.04 redefinition root-caused + fixed in restore_frozen_seeds,
    (next/cap/used already exported) — the seed's block-chain iteration surface.
WIPE-OK: tape+inbox+INDEX retirement (md 단일화) — content folded to embedded.gen.hexa / CLAUDE.md / CHANGELOG+git; build-inert (codegen·runtime 비참조 확인)
RT-NATIVE-ZEROC Route B measure-6: val-arena C-ABI bridge (HexaVal 2-reg → plain-C) — tag3-tag0 miscompile SOLVED, wall → seed global-tag codegen (#3544)
fix(codegen): cover full <math.h> classification-macro set in user-fn name mangling + regression test (#3534)
docs(governance): self-host must never regress the shipping release (CLAUDE.md rule + CHANGELOG) (#3582)
fix(codegen): darwin-arm64 try/catch silent-death — setjmp/longjmp pair mismatch root-cause (#3576)
- 루트 CLAUDE.md: 구조트리 ATLAS/ + ATLAS math-map 거버넌스 — 수학 DFS 는 `hexa loop --dfs`
- ATLAS/CLAUDE.md: 제목/경로 자기참조 → ATLAS, DFS 룰 hexa loop --dfs 로 갱신
* ci(release): add install.sh-path runtime-compile gate (clang-18/24.04 redefinition)
RT-NATIVE-ZEROC Route B measure-5: heapify block-walk wired (str-arena native CLEAN), val-arena residual → wall re-classified (#3543)
  DFS 이 기저에서 닫힘(closed). 추가산출은 무한팽창(코어×구조사실 합성)이지 새 수학 아님. ∎
* RT-NATIVE-ZEROC r3 map_set = C orchestration; Route B (arena) is the real next ls lever (F-M5-R3-MAPSET-ORCHESTRATION)
  박제 대신 실질 가이드로 작성 (root ../CLAUDE.md 우선 · 구조 안내 · 검증/정직 룰 보강)
  구조적 🔵 = 합성수 타깃 {4,6,9,12,14,18,28,35,72,108}. 전부 정확검증 N=5e4 bounded-unique,
fix(release): install.sh clang-18/24.04 redefinition (restore_frozen_seeds scrub) + recurrence gate (#3540)
유저 지시: TECS-L/ 를 ATLAS/ 로 개명 + DFS 는 hexa loop --dfs 로 진행, ATLAS/CLAUDE.md 기록.
hexa atlas 모두 등록 — 정식 PR-fold 경로(.n6 생성 금지·embedded.gen.hexa 가 기계 SSOT):
* docs: Route B linked-block+seed-regen DONE (ARCHITECTURE/README) + re-drop README NOTE (merge-revert)
body, so the failed assemble fell through to "C arena" with no body → unresolved
* docs(rt-native): measure-6 — Route B val-arena C-ABI half SOLVED (tag3-tag0 + recursion gone), wall re-classified to seed global-tag codegen defect
* docs(rt-native): Route B measure-4 — link/ABI/RSS PASS, heapify slow-path blocker (CHANGELOG/ARCHITECTURE/README/ING)
fix(x86_64 codegen): assign-to-global tag-slot store must precede writeback (measure-9 tag-corruption root-cause) (#3551)
fix(release): build linux binaries on glibc-2.35 floor (ubuntu-22.04) — support 22.04 cloud pods (#3559)
fix(release): linux-arm64 runner 22.04-arm → 24.04-arm (restore v0.241.0 arm64 asset regression) (#3591)
feat(deck): el-ph 디스크-안전 q-배치 러너+짧은런처 emitter (d_deck_always) (#3581)
(the block* field is meaningless in the pool-table model). So Route B's C-ABI half
it, so self-emit is unchanged (gen3 == gen4 preserved; CI byteeq-real authority).
fix(runtime emit): hexa_arena_on undeclared at runtime_core.c:4859 — decl-ordering fwd-decl + restore C arena #else body (zero-C/self-host byteeq blocker) (#3583)
RT-NATIVE-ZEROC Route B measure-7: x86_64 module-global tag persist + regen .section .data — __env_extend RC=139 wall CLEARED (#3546)
docs(atlas): draw 거시↔양자 map + 🏝️ 단일섬 registry + 🔵 closed-form 승격 (#3556)
docs(governance): record atlas math-map rule in CLAUDE.md (atlas/README.md = 단일 원장) (#3555)
    · 기저확장=무한팽창 ⇒ 구조적 광맥 = {6,12,28} 곱-특성식, 2-term DFS 고갈.
유저 요청 — archive 를 gitignore 할 필요 없음. 범용 `archive/` 무시 규칙(191) +
* RT-NATIVE-ZEROC: alloc-r2 NO-GO 확정 + M5 alloc 첫 blocker 식별 (F-M5-ALLOC-CIRCULAR-DEP)
동기화 — pool 빌드/byteeq/measure 가 stale prebuilt 를 물지 않도록(c2). 이번
ORDER RE-CORRECTED: Route B (arena full-feature -> .hexa) is the REAL first substrate