fix(selfhost zero-c RFC061 · dropon-runwall): strarr-read multidef 가드 누락 emitter-route 해소 — drop-ON link multidef 2→0, run-wall FALSIFIED (segfault 아님) (#3919)
(특히 Dirichlet 합성곱 f∗g — 현재 점별-곱 스윕이 통째로 놓치는 backbone). embedded.gen.hexa
eqtruthy 교정과 직교) → smoke RUN 은 LINK FAIL(non-fatal) 로 정직 보고하고 PRIMARY
19-fn product frame (1.15M scans) → only J₂=φ·ψ (classical) + core·rad=n (definitional).
byte-eq-safe: 비-CUDA(default/REF/CPU 출하) 빌드는 전처리기가 #ifdef HEXA_CUDA 통째
지속 QA 거버넌스(#3922)에 polarity 위반 감사를 명시 추가 — QA 루프가
legal) — the research hypothesis "-fno-common multiple-definition" is FALSIFIED
Honest false-positive filtering (μ/μ² zero-factor cancellation). Next = Dirichlet convolution frame.
measure(selfhost zero-c RFC061): map-colocate lever-a FALSIFIED — drop-ON wall is single Diagnostic value-confusion, not cross-TU map desync (#3896)
fix(selfhost): hexad full-canonical — shim hexad-direct exec + flip 센티넬 hexa.real→hexad lockstep (byteeq-neutral) (#3936)
NaN서 0.0 wrong·큰 |x|서 near-hang. floor/ceil/round 전부 trunc 위임이라 가족
* feat(forge matmul): GEMM 출력을 _d2h_out disposition 핸들러로 라우팅 — DEVICE_KEEP 존중(decode round-trip 제거)
research(atlas): multiplicative-identity literature note — J₂=φ·ψ cite + next-frame catalog (#3908)
갱신하지만 dirty_host=1을 안 켠다(hexa_farr_set 계약과 달리). 다음 forge op의 _h2d가
origin/main(ec01507bb · #3897/#3899/#3915 포함)에서 재측정 → segfault 가 아니라
(~13k/frag)이라 매 GEMM마다 host 왕복 = util sink의 핵심. 다른 모든 forge op
실 registrar 면 no-op 스텁 없이 link. ZEROC_M7_REAL=0 = 레거시 대조 모드.
* feat(selfhost zero-c RFC061 Case-B): HEXA_RT_MULTIOBJ opt-in gate — 출하 runtime.a drop-ON 멀티오브젝트 링크 경로 (default-OFF·byteeq-neutral·frozen 불변)
feat(atlas): census fleet r1 — @N fold-prep(1654) + 3-factor grid(1431) + multi-⊞ all-coincidence (#3902)
novel; grade verified:false until a real `hexa verify` certifies it — no forced 🔵). New
emitter externalize 는 이미 #3894 로 origin/main 머지·CI-green(출하 byte-neutral);
측정근거(nsys·aiden RTX5070): 단일토큰 decode = memory-bandwidth-bound(63% mem
reference-match QA 전부 faithful = 메모리에러도 커널 math 버그도 아님).
feat(selfhost zero-c RFC061 Case-B): HEXA_RT_MULTIOBJ opt-in gate — 출하 runtime.a include-drop 멀티오브젝트 (default-OFF·byteeq-neutral·aiden GREEN) (#3935)
fix(build-path+cloud): stale runtime.a installed-link sync (①③ forge_dispatch undef-ref) + cloud copy-to recursive contents-merge (② core/core nesting) (#3889)
fix(naming): install.sh/install.hexa/build_hexa_cli를 hexad-canonical로 수렴 — consumer 설치경로 drift 교정 (byteeq-neutral) (#3933)
docs(audit): archive-TECS-L unified-field corpus — verified-vs-numerology honest audit (13 VERIFIED · 48 NUMEROLOGY · 5 OPEN) (#3913)
feat(atlas): fold J₂=φ·ψ @F atom (first machine-discovered ∀n node) + implement resolve --fold @L census-summary (#3910)
* docs(CHANGELOG): RFC061 Case-B 엔트리를 minimal include-drop 교정형 + aiden 측정 GREEN 으로 갱신
* fix(restore_frozen_seeds): awk print \& → & (gawk 이식성 — stray backslash가 주입 C 깨뜨림)
universal-hunt mechanically (re)discovered the Jordan-totient identity J₂=φ·ψ — the atlas's first
RTSC 전부 absorbed=false/GATE_OPEN — 자율 in-silico 루프는 발견 제조 불가, honest-cap.
발산 → argmax flip → 게이트 붕괴(anima decode ON!=OFF). fix: 두 런치를 forge stream
문자열에서 & 는 리터럴(sub/gsub replacement서만 특수)이라 백슬래시 불필요.
trunc_pure 반복뺄셈 루프가 isnan/isinf 가드 없어 ±Inf서 무한루프 hang(DoS급)·
roofline), GPU util 0-9%는 커널 탓 아님 — hexa farr host-dispatch + per-call 왕복이