generated (the emitter is the SSOT; no manual File-B edit). Verified by PRE-PUSH regen + default
feat(selfhost zero-c #29 RFC061): wire strarr-read cluster into build_aprime.sh (#4342)
siblings that would multidef the shim — integer-leaf canon), adds it as an archive member, and drops
to_string path). Active SSOT runtime_core_emit.hexa → regenerated runtime_core.c,
(#4285 / f8e846c6b, v0.499.0+); this hardens the install path so the fix actually
bodies. The #ifndef HEXA_RT_CORE_STRARR_READ_NATIVE guards are already emitted (runtime_core_emit
Extends the #4288 raw-svc pattern; musl reference-match. The #ifndef Route-C gate is kept (no
T _hx_sin, hxlcl_shim.o references U _hx_sin, resolved within runtime.a (same pattern as the
ship-path exit=0, 0 errors; arm64 fsqrt inline asm compiles; changed buf-lines end
emitter-resident (no frozen .s seed; mini-editable; same pattern as #4288-#4291).
#3798's measured proof). flag-ON -> resolves rt_strcmp (def at 768 precedes this callsite, same TU,
session (#4294/4295/4298). The #ifndef HEXA_RT_NATIVE_X gates stay (Route C native .o drops the
GREEN (#4299-4318 + #4314). Residual now = GPU FFI + syscall remainder + C runtime substrate.
docs(selfhost zero-c #29): substrate-reduction milestone + honest floor census (#4338)
drop-measure regen — one more irreducible category cleared toward the all-or-nothing #include flip.
Corrected a bug in the workflow's shim edit (it emitted the opening #ifndef with no matching #endif =
(7), and reducible-remaining (~13). The reducible fs/time syscall front is effectively closed this
merged value-op PRs #4299-4313; multidef-safe). _hx_* are C-mirror libm-bit-exact verified this
updated: syscall remainder removed -> CRT bootstrap + CUDA/dlopen FFI + functional-libc leaves.
seed .o (self/native/rtcore_strarr-read_emit.hexa SSOT) instead of compiling the inline #else
seed-o): the flag-ON path ALWAYS regenerates the .o (idempotent, self-verifies 2/2 nm), so a
the libm-free proof lands at #4314 rebase. With fmod (#4315), the shim is now fully native;
darwin/other keep the libc delegate (#else). Self-contained inline-asm, same pattern as the
(fdlibm/musl/hw sqrtsd) so no recursion back into libm. This is the robust root-cause fix
Single-line \n buf-strings, no internal newlines (convergence runtime-emit-full-hexa-1).
darwin/other keep the libc delegate (#else). Self-contained inline-asm, same pattern as
stays a single emitter buf-line ending in \n (convergence runtime-emit-full-hexa-1).
cancellation). __builtin_fma is NOT used — it adds an undefined libm fma symbol at
0 direct pow remain; buf-lines end \n". STACKED on #4295 → rebase+PR after merge.
Both native impls pre-exist (R3); this is wire-to-prod (dead libc in the JSON path
First real libm-frontier drop, verified by nm -u (not call-token). Replace the 11
Verified local: emitter regen → clang -fsyntax-only ship-path exit=0, 0 errors;
Verified local: emitter regen → clang -fsyntax-only ship-path exit=0, 0 errors;
emitter-only libm drops are currently inert in the prebuilt-runtime.a ship path).
- runtime_emit_full.hexa carrier (HexaVal entry, #ifdef HEXA_CUDA device dispatch
no forward-decl, no new seed). This makes __raw_cmp3's callee tree frozen-static-free under the flag.
RFC061 §M8 Route C strcmp dissolution (terminal strcmp win, workflow wf_74662806; mirrors the merged
RFC061 (A)-axis setjmp-wall de-risk (deepdive workflow wf_61a7dbb5 safe rung). Gate the 4 try-frame
'return fmod(x,y)' is demoted to a -DHEXA_FMOD_LIBC opt-OUT escape hatch (native-canonical-default
sole remaining irreducible libm-transcendental (its #ifndef HEXA_HAS_HEXA_RT_STDLIB branch called
Honest value: linux gets ZERO benefit (its try-path setjmp.h is anchored in the frozen runtime.c
Reduce the runtime.a substrate's direct libc calls (first symbol after the true nm-UND census).
+ amalgam static stay link-satisfied by the native T hxlcl_strcmp; a fully macro-free flip is a
cos/sin/pow = accuracy-gated follow-up (fdlibm-native + perf verify); all-native → -lm drop.