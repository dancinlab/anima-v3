gen_auto_ideate undeclared 는 codegen 불가능, 오직 stale 모듈 스냅샷(install overlay shadow)으로만
broadening 정의차). 별도 .elph fixture 파싱으로 σ=0.010서 8.516825 비트 재현.
* docs(hexa-own): Step0 defer 실측 종결 — C4 divergence 판정(native 규범발화 vs gen2 미발화→gen2 결함 처분)+부수결함 3종, L1 셀 lockstep
forward-decl 루프는 flattened AST 의 모든 FnDecl 에 gen2_fn_forward 방출 + arch 분기 없음 →
* docs(hexa-own): fix 최종 실측 캡처 — 3판정 PASS+14게이트+aggregate 미배선 규명
reference 숫자 미변경·순수 (σ,mesh) 라벨 보강. d6: tight-σ 단일점 강제 금지.
  (gen_auto_ideate L3 단일진입점; gate FAIL=gen4/no-corpus/smoke모델 정직 verdict, 결함 아님)
matched-σ로 대조하면 QE@0.020 대비 −1.35%(박제됐던 −5.47%는 tightest-σ 대본
byte-identical. The comment's 'DEFAULT unset = byte-identical' release-integrity invariant
fix(release cuda-verify): install cuobjdump toolkit sub-package — #4404 PTX guard died command-not-found, cuda asset missing since v0.553 (#4423)
docs(hexa-own): L3 borrow census 박제 + architecture-json-1 4차 재발 현행화 + plan/ING 동기화 (#4488)
feat(QFORGE): from-scratch frozen-phonon Γ-포논 프론트엔드 (g5 PASS 13/13) (#3619)
fix(release): explicit Latest-marker promotion + verification in finalize — softprops make_latest can silently not take on the update path (#4478)
* feat(qforge): block-GEMM Davidson el-ph hot path 배선 (orchestrator_pw + gga_scf) — R2 GPU 실측 PASS (#3663)
합격선 (1) 컴파일 RC=0 PASS (2) metric-surface parity PASS · GPU own-GEMM DEVICE 발화. codegen 축
terminal-GREEN. 잔여는 anima decode perf/leak 벽(ING#21/#22), codegen 무관. 코드 변경 0.
perf(run): gate the native --emit=obj path to Linux x86_64 hosts — kills the doomed 24min cross-target compile on darwin + forward captured output under HEXA_CG_PROFILE (#4483)
perf(codegen gen2): replace _strlit_dedup O(L^2) linear scan with 64-bucket hash index; join()-based '+'-chain flatten body — emitted C byte-identical (#4454)
* docs(convergence): ci-pick-runner-sh-1 — darwin runner queue starvation lesson (busy-blind routing x merge-train x repush reset)
frozen-seed variant (runtime_emit.hexa:1426/1432) KEEPS libc so a frozen runtime.c still links. Remaining U
같은 레코드 ladder는 σ=0.020→4.193·0.025→3.717. QFORGE bare 4×4×4 λ=4.137을
- 3a-4 g_gates_smoke: 14 PASS/0 FAIL RC=0 (7 hexa<->py reference-match parity case 포함)
fix(release cuda-asset): embed PTX in runtime_cuda fatbin — sm_80-SASS-only asset silently no-ops on newer GPUs (#4404)
docs(qforge): CaH6 el-ph λ answer-key 정합 — 4.376은 σ=0.015 단일점, matched-σ서 QFORGE −1.35% (#3646)
docs(convergence): install-sh-1 + selfhost-byteeq-real-yml-1 (verdict-integrity/DX lessons) (#4442)
fix(ci): align main-push cancel policy on selfhost-byteeq-real + selfhost-gates-required (#4463)
perf(codegen): FNV hash index for both backend strtabs + join()-based per-literal folds — byte-identical (#4454 parity for aprime) (#4462)
* feat(hexa-own L2.5): arena-escape lint HX3013 — global에 composite 저장 경고 (stacked on #4467+#4470) (#4477)
* feat(cuda #21 gold-v2 RANK-3): 4-row j-unroll fp32-exp attn variant (bit-identical, opt-in default-OFF)
- 2 hexa build cli/anima.hexa: RC=0 undeclared=0 → native binary, clang cuda-link 성공(#4028 live)
no dlsym line in main) + a native-call wrapper preserving the exact static HexaVal cname(HexaVal...)
유한변위 Hessian→ASR→ω, 해석분산(2원자사슬) 대조 ω_opt=√3 rel-ε<1e-5.
docs(zeroc #29): dlopen WALL 발산 돌파 — floor→0 도달가능 (구조변경) (#4475)
Ground truth: zero .github/workflows runs-on: blacksmith (all ubuntu-*/macos github-hosted;
* docs(hexa-own): L1 셀 = MERGED(#4446)+Step0 실측 QA(C4 divergence 처분 b) 최신화
Blacksmith migrated in #4015 then reverted for cost in #4016; PR gates run github-hosted.