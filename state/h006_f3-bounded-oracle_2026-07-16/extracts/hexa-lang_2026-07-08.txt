Ranked next fix ③-R2a = integer-ALU encoder completion (imul+bitwise+shifts+neg/not/movsx) + a loud
docs(hexa-own): FLIP-6 native reclaiming heap allocator design (axis-② terminal flip) (#4723)
Sizes the axis-③ COMPILE-half endgame (own-emit --emit=exec default-flip). The x86_64 own-emit encoder
:718 → zero bytes, no diagnostic — worse than arm64's ENCODE-MISS print) → corrupt text, not a build
heap allocator behind HEXA_RT_HEAP_NATIVE that drops malloc/free/calloc/__libc_calloc/__libc_free
docs(hexa-own): axis-③ R2 own-emit x86_64 COMPILE coverage census (#4727)
ENCODE-MISS abort; SSE-float = ③-R2b. NOT an architectural wall — an enumerable encoder backlog. Doc-only.
Fable-designed FLIP-6 — the last reducible libc family for axis-② (no runtime.c): a native reclaiming
mmap-per-alloc/munmap for large, emitted as a C body in the shim TU behind -DHEXA_RT_HEAP_NATIVE, keeping
구현됨·미배선 (unwired): called by NO shipping path yet — Stage 4 (link_elf_x86_64_ownstart integration)
(runtime.a doesn't self-satisfy malloc/write/mmap) = the axis-③ sanctioned floor, not literal 0-UND.
POOL-VERIFIED on summer: DATARELOC 8/8 PASS (link rc==0 · e_phnum==2 · p_filesz==36 · p_memsz==52 bss+16
feat(check): 2-lane batch — HX3044 index/field-place assign fan-out + parse_int/float 추론 (#4722)
error. Gaps the codegen emits but the encoder drops: imul (21x), bitwise and/or/xor (200x), shifts, neg/
'#1/NN' or '__.SYMDEF' first member) since GNU-only is correct for the axis-③ x86_64-linux Linux-pool
arms preprocess away, _heap_def="" -> runtime.a sha identical -> byteeq trivially GREEN); PR-B = 1-line
from rdx) = byteeq-RED default-path change. byteeq-safe: link_elf_x86_64_ownstart is opt-in only
feat(types ① HX3044): STRICT-Error re-band + 5-site fan-out + negative-literal (#4708)
- (A) link_elf_x86_64_ownstart_ar(program_objs, out_path, main_name, runtime_a_path): read_file_bytes
docs: ELF axis-③ campaign complete — round-3b data-arm coverage runtime-verified (#4718)
This is the round-3 SSOT for axis-② lane of the self-host DONE 3-axis fleet. Doc-only.
* fix(mem ② RUNG-2a): depth-guard the whole-body IP scanners — sound ⊤-fallback on deep bodies (gated, byteeq-neutral)
구현됨·배선 (D wires the dispatch into the --linker=hexa build path). e2e fixtures in state/hexa-own/round3_e2e/.
feat(mem ② P0): arena loop-body reclaim WIDENED characterization — real-churn verdict = PIVOT-TO-RUNG2 (#4703)
- CLAUDE.md: an ACTIVE GOAL line in the SELF-HOST DONE CRITERION section — the fleet + axis-③ LINK-half
not/movsx, and the ENTIRE SSE float family (no xmm table). default-flip readiness ~0-5% of real programs.
is a ~700-line integer subset (elf_x86_64.hexa:322-718); it SILENTLY drops unsupported ops (return [] at
link_elf_x86_64_ownstart were compiled + correct-by-inspection but not runtime-EXERCISED (the round-3
So own .o + runtime.a -> own linker -> running static exec = the axis-③ self-link demonstration.
completion via a perpetual 3-lane fleet (selfhost-done-3axis-fleet). Recorded prominently:
(link_elf_x86_64_ownstart_ar) so the own `.o` + runtime.a link into a static
· A1 rodata-abs64 · A2 data-abs64+8 · A3 data-PC32 · A4 abs64→bss · B1 cross-obj abs64 at obj_data_base[1]).
* feat(mem ② RUNG-2a): inter-procedural callee no-escape SUMMARY characterization (report-only, byteeq-neutral)
User set the standing goal: drive self-host DONE 3-기준 (①no hexa_cc.c ②no runtime.c ③no clang) to
external .o members). ELF axis-③ self-link (round-3 core + round-3b + coverage) fully landed. Doc-only.
- ARCHITECTURE.json: new top-level activeGoal (definition · driver · per-axis OPEN status · guardrail)
* docs(mem ② RUNG-2a): summary lattice + decision measurement + go/no-go = TERMINAL-NOT-ARENA-PROBLEM
once). Sanctioned libc floor names (malloc/write/mmap…) remain UND with no error (axis-③ floor for
(compiler/codegen/x86_64_linux.hexa) returns the 16-byte PAIR-MODEL HexaVal in the SysV integer pair
x86_64-linux only and gated on env HEXA_LINK_HEXA=1, invokes the native