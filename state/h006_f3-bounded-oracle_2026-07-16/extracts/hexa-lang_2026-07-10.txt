_lr_fn_ref_arity (hir_to_mir.hexa) is the ONLY genuinely module-wide-UNRESET O(N·F) scan: _lr_fn_ref_names is
perf(lower/hir_to_mir): hash-index _mir_lookup_global — kill whole-module O(refs·globals) scan (#4773)
- compiler/lower/hir_to_mir.hexa: field-set + index-set arm hooks (base immut-let
Advisory (never-gates) sibling to the nm-UND floor dump in nobaseline-gate.yml.
# no-verify-ok pre-existing ARCHITECTURE.json lint · byte-neutral flip PR-gated on byteeq-real
# no-verify-ok: diff=CHANGELOG+state/ runbook 2개만 · ARCHITECTURE.json 무관(33 lint 위반 전부 origin/main 기존부채, 본 변경과 무관)
perf(lower/hir_to_mir): bucket _lr_fn_ref_arity — axis-③ self-emit round-4 RANK2 (#4795)
  pack_map floor) right-size above this; est −448…−672 B/instance table slack.
    bodies (TAG_ARRAY_I64 → boxed once, then existing byte-copy) — compiles to
the site at placeholder 0 → si_addr=NULL SIGSEGV at run (Fable H1 · matches the
C-transpile backend build_aprime.sh stage-2 uses) lower an EMPTY type-annotated
fix(emit/x86_64): axis-③ own-link 정확성 3-fix (_start argc/argv · H1 unresolved-reloc 진단 · H2 STB_LOCAL scoping) (#4828)
perf(runtime): heapify watermark fast-reject — 12.8× lower_ast_to_hir (HEXA_ARR_HEAPIFY_WATER, default-OFF · rung#1b) (#4798)
# no-verify-ok: MEASURE-ONLY verify tool + CHANGELOG; ARCHITECTURE.json untouched (pre-existing big-cell debt orthogonal)
the linear scan's first-match → identical arity returned → byte-identical MIR → gen3≡gen4. The other round-4
dynstr/dynsym/DT_HASH/rela.dyn/_DYNAMIC/PT_INTERP 전부 data_bytes 꼬리 + serialize_dyn
rt_str_parse_float_exact — the existing float_dtoa_shortest_gate.c checks with
  routing; binder/scope/lexer [Int] params receive boxed arrays → corrupted.
debt on origin/main, orthogonal to this convergence-edit + verify-doc change.
convergence codegen-hexa-2 updated with the inert-lever + real-wall finding.
# no-verify-ok: 33 blocking violations are ALL pre-existing ARCHITECTURE.json big-cell/piled/malformed-convergence debt — orthogonal, ARCHITECTURE.json untouched by this CI-yml+CHANGELOG change
feat(emit/x86_64): axis-③ rung-1 dynamic-link (GLOB_DAT-only + 6B stub) — sanctioned libc floor → libc.so.6 (#4830)
The session-close workflow (Lane B) ranked the surviving self-emit superlinear phases after #4779/#4782/#4784.
# no-verify-ok pre-existing ARCHITECTURE.json big-cell/piled violations unrelated to this lowering perf change
land separately. Note: the self-emit .o-produced re-measure is blocked by an aiden infra wall (a runaway
Byte-eq NEUTRAL by construction (same id returned). Compile-time O(N²)→O(bucket)
debt on origin/main, orthogonal to this build_aprime.sh guard + review-doc change.
  free-never — hexa_map_remove_impl (:4722 + shard rtcore_collection-mutate:216)
# no-verify-ok WIP flag-OFF (byte-neutral) + pre-existing ARCHITECTURE.json lint
global, (C) per-fn name set, (D) TU reset ×2, (E) param-block gate + falsified-
  each fn's `param_names` annotation (index-aligned with sig_params) and record
param + empty-typed [Int] let across ALL self-compiled sources (atlas/borrowck/
existing _selfemit_pack_out_enabled() gate (unset in release build_aprime); the
closure-lower site and the closure moves it into an `@own` param, emit the more
  L2 flat-struct lowering (HEXA_STRUCT_FLAT, ~4× + attacks the codegen wall),
Opt-in borrow checker (HEXA_BORROWCK, default-OFF -> byteeq-neutral). Adds the
(hir_to_mir.hexa:1200-1210) is flow-insensitive (no NLL last-use kill), so it
- → LIR+HIR ≈ 2/3 (plan correction: NOT the predicted LIR+MIR; MIR is now
whole-module O(refs·globals) that the aprime_cc --emit=obj self-emit on the
violations on origin/main, orthogonal to this CI-hygiene + state-doc change
stage_resolve_runtime_a:2018 ${HEXA_RT_NATIVE_STRTOLL:-0}→:-auto: with a target frozen seed present, the shim libc strtoll member is replaced by the native 1-symbol seed (drops strtoll from the nm-UND floor); missing/bad seed → loud WARN + shim fallback (non-fatal, graceful).