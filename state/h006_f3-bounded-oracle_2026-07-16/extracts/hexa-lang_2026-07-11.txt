compiles). Design: Fable verdict B (A/C rejected) + measured variant (skip fflush redirect
  link_elf_x86_64_ownstart (new runtime_defs param; plain callers pass [] →
the C-transpile delegate only on failure. The comment misrepresented the default
# no-verify-ok ARCHITECTURE.json lint violations are 100% pre-existing baseline
# debt (30+ oversized/piled cells + malformed convergence) NOT touched by this
# no-verify-ok: .github + CHANGELOG staged explicitly; pre-existing ARCH big-cells are base-merge content.
existing-GREEN output is unchanged; the plain static/round-1/2/main.hexa link path
the native path before, so existing-GREEN byteeq artifacts are byte-unchanged.
before, so existing-GREEN byteeq artifacts are byte-unchanged; gen2 C path
# no-verify-ok: docs-only (CLAUDE.md + ARCHITECTURE axis2 status + CHANGELOG); pre-existing
link_elf_x86_64_ownstart census(ddef)+resolve(H2) per-reloc O(defs) 선형스캔 3-4중첩
default, C-transpile graceful fallback-only. Blockers resolved: (a) miscompiles merged
STDIO_NATIVE switch fixed the EXIT_FLUSH SIGSEGV); this unblocks the T0-T4 determinism checks.
def exists, so the C-2 own-link tripwire is the correct net (documented in place).
  landmine since fflush is NOT redirected; grep: all 91 fprintf sites are stderr).
Lockstep the CLAUDE.md measured-tracks flip list + ARCHITECTURE activeGoal.status.axis2.
are source/CI edits pending pool byteeq 3-target + self-emit own-link exercise.
The perf fix + measurement (>90min->106s) are documented in the parent commit
  the tripwire can never false-refuse a valid libm/mem/io floor link.
runtime.c->0) + closing the own-start+explicit-exit buffered-print loss (measured #4850/#4852).
axis-③ end-to-end(self-emit .o+own-link·ARC_E2E=PASS_BYTEID) 종결 후 최대 C-fallback
axis-③ end-to-end(self-emit .o+own-link·ARC_E2E=PASS_BYTEID) 종결 후 최대 C-fallback
census expected NEW=0. Measurement moved to healthy github CI (selfhost-native-build-gate +
print(hi)+exit(7) survives → the gate executes. Advisory. Transitivity: own-linked
own-start + explicit exit() loses the buffered tail. summer-measured; fix #4852.
x86_64_linux.hexa inherits every mapping with no call-site change. ADDITIVE —
as opt-in. Found by the axis-③ own-link default-flip census. Comment-only →
  self-emit own-link step deposits a binary; catches any future emit path that
# change (ARCHITECTURE.json is not staged); this is a guard/CI/codegen change
untouched. Design: state/hexa-own/axis3-symcensus-drain-design.md Rung B.
(6 D-state zombies 20h+). post-merge healthy-host census re-run advised. Design: Fable wix4sm9xw.
* perf(emit/elf): axis-3 own-link 1024-bucket def-index + per-obj sym-name cache — >90min->106s
C-transpile delegate from the default (axis-① DONE-criterion). GRACEFUL: C-transpile fallback
inline #ifdef on each of the 4 function signatures (external ON / static OFF) — the same
raw exit_group runs no _IO_cleanup). Wired _zc_exit_flush to all runtime.c compile sites
pattern as the existing '#if defined(__x86_64__)' emit blocks, which survive synth. No
The prior #4850 branch was created off a divergent local history (no common ancestor
# with no structural SSOT delta. L0 main.hexa handled deliberately + changelogged.
Measured (#4854 run 29151569223): HEXA_ZEROC_EXIT_FLUSH=1 makes hexa_v2 SIGSEGV at
  (runtime.a def / drop-list gap). Diagnose+refuse only — never re-resolved, so
untouched. Build gate: mini read-only (no build); C-1 verified locally, C-2/C-3
was already benign — this removes the same asymmetry from stage_build_hexa.
Signature untouched (1-arg): the table is shared by both native backends, so
bit-changing on x86 (const_void tag imm 0 → 4); arm64/nvptx untouched.
measurement 2026-07-11, rc127). Root-cause: the UND originates in the
byteeq-neutral(own-link opt-in path·shipping 무영향).
gen3≡gen4 = .o byteeq gate ∘ deterministic own-link.
compiler/emit/elf_x86_64.hexa
compiler/emit/elf_x86_64.hexa
fix(axis3): own-link fail-close no-runtime.a + argv map + C-4 libgcc-forbid CI gate (RUN closeout) (#4846)