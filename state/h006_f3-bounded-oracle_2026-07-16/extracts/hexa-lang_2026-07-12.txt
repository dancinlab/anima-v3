# no-verify-ok byteeq-neutral inert prep (신규 flag default-OFF+빈 allowlist=behavior 무변경); ARCHITECTURE.json ARCH-BIG-CELL/PILED/CONVERGENCE 위반 33건은 이 변경과 무관한 pre-existing 부채(어떤 커밋도 차단)
# no-verify-ok surgical .trim() fix on a default-OFF gated code path; ARCHITECTURE.json refresh N/A (no deep-structure change), the other 33 hits are pre-existing arch cell-size/convergence state untouched by this change.
feat(axis-③/PR-2): own-link default-flip DEFAULT-ON (linux-x86_64, opt-out =0) (#4882)
HEXA_RT_GMTIME_NATIVE default-OFF → runtime.a byte-identical(byteeq-neutral). hxlcl_gmtime_r_compute
feat(zeroc L3 axis-②): stdio FILE* MULTIOBJ swap — HXLCL_STDIO_LINK 게이트(default-OFF·byteeq-neutral) (#4894)
AArch64 twins of the x86 archive-linker parse/extract helpers (elf_x86_64.hexa:3826/4297),
# 잔여 1건(ARCHITECTURE-MISSING)은 default-OFF byte-neutral 컴파일러 플래그로
feat(axis-③/PR-1): own-link default-flip opt-in leg (HEXA_OWNLINK_DEFAULT=1) (#4877)
fix(link/ownstart): axis-① bug3 근본 — archive_extract_fixpoint가 STB_LOCAL floor body pull(own-link) → n_dyn→0 static [DRAFT·pool검증대기] (#4871)
S0 (verified): this branch is on origin/main (5493-line elf_x86_64.hexa; the plan's anchors
docs(axis-③): preserve own-link default-flip campaign design artifacts (state/) (#4883)
Durable state/ record for the axis-③ own-link default-flip campaign (6 PRs landed
* # no-verify-ok rung-3 설계는 campaign SSOT(state/hexa-own/axis3_arm64_ownlink_campaign.md a33a60067)+convergence asm-hexa-1(f771cd5c1)+방금 CHANGELOG로 문서화. ARCH-BIG-CELL/PILED 33건은 기존 트리 부채(이 codegen 변경 무관·surgical).
* docs(architecture): sync axis-③/axis-① goal status — own-link default MEASURED clang-0 on the real install + marker-irony prerequisite
전부 _bck_active/_bck_on 게이트 → default-OFF byteeq-neutral. 예약어 `own` 미사용.
region) never defined them. Fix mirrors x86 elf_x86_64.hexa:2149 — register both GLOBAL
# no-verify-ok 21 lint hits are pre-existing ARCHITECTURE.json big-cell/piled debt on origin/main; this diff (5 files) does not touch ARCHITECTURE.json.
axis-③ arm64 own-link rung-1→rung-3: Road A dynamic-link (opt-in --linker=hexa, x86-parity verified) (#4896)
# no-verify-ok: pre-existing ARCHITECTURE.json lint (unrelated to this convergence edit)
# no-verify-ok: the 33 ARCH-BIG-CELL/PILED/MALFORMED lint hits are all PRE-EXISTING
fall-through. Distinct from ENCODE-MISS=0 + corpus-parity: proves the CLI cmd_build
feat(borrowck L5-2b): HX3014 gen/kill 정밀화 — swap 관용구 FP 제거 [default-OFF·byteeq-neutral] (#4889)
feat(axis-③/PR-1): own-link SHT_INIT_ARRAY infrastructure — static-path ctor execution (dyn inert) (#4878)
(link_elf_x86_64_ownstart + archive_extract_fixpoint #4871 + GOT synth + CRT1-HANDOFF), and
archive_extract_fixpoint_arm64: iterative member-pull (GLOBAL/WEAK armap wants + the #4871
* fix(link/ownstart): axis-① bug3 근인 종결 — extract-fixpoint STB_LOCAL hxlcl_ floor-pull + census data-driven alias → n_dyn→0
* docs(axis-③/arm64): rung-3 RESULT 박제 — Road A dynlink WORKING(qemu·x86 parity)·rc=0 shared follow-on
* docs(axis-③/arm64 S2): MEASURED — arm64 native seeds all exist + assemble (rung-4 highly feasible)
runtime.arm64.a → archive_extract_fixpoint_arm64 pulls the members satisfying the program's
let→let mut는 checker annotation 변경으로 codegen-neutral(mut은 다르게 emit되지
let→let mut는 checker annotation 변경으로 codegen-neutral(mut은 다르게 emit되지
STB_LOCAL hxlcl_ floor-pull, which keys on symbol names only → carries verbatim) via a new
r1(unbox-hir-calltype-r1)이 단일커널 k6=0.750× CPU 실측했으나 GLOBAL default-ON
Two byte-neutral shell hooks so a Linux-x86 host can emit build/runtime.arm64-linux-gnu.a
best-effort (own-link is not their default — a build failure degrades cleanly there).
so they were green — the #4882 default-flip was false-green on the real CLI ladder.