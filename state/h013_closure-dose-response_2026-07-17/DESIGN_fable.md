# H_013 closure-dose-response — Fable 5 design (seed of record · distil FROM here)

Campaign synthesis + H_013 spec (is the F8 proxy anchor DOSE-ROBUST or TOY-FRAGILE?)

```json
{
  "campaign_read": "Reframe C is refuted on the real owner-loop at every $0-reachable lens: the 2x2 target×timescale matrix (text/behavior × one-step/window), each estimator plant-certified in-regime, all refuse — the agent's utterance does not shift the owner's subsequent input beyond context, and each refusal independently lowered the H_012B prior. The proxy anchor (H_011 7B, LV-C 0.76, 5/5, Watson-yoked) is real and certified, so closed-loop causation EXISTS as a measurable property — but where it was found is an env-loop (F8 micro-tenant), not the owner relationship (F7). The honest read: aliveness-as-closure currently lives in the environment-loop substrate, and its presence in the owner-loop now rests entirely on the untested sequence-contingency residue, a channel whose prior is low but which only H_012B's real randomization can anchor or interventionally refuse. The open question is therefore no longer 'does closure exist' but 'is the F8 closure substantive or an artifact of a deterministic env that mechanically answers actions' — i.e., tension reading (ii) is now the load-bearing uncertainty, and it is $0-testable. Nothing autonomous can touch the F7 residue; everything autonomous should interrogate the F8 anchor's robustness.",

  "is_reframe_c_dead": "ALIVE-BUT-RELOCATED. Closed-loop causation anchored in the env-loop (F8) under a certified yoked-control gate, so the thesis is not falsified as a seat-of-aliveness criterion; it is refuted for the owner-loop (F7) at the single-utterance and utterance-to-window level across both targets, with only the sequence-contingency channel formally open (H_012B-gated, low prior). Treat F7 as PRESUMED-REFUSED pending H_012B; treat F8 as the live seat — conditional on surviving a toy-artifact test it has not yet faced.",

  "next_direction": "(a) Return to the env-loop/F8 substrate — but adversarially, not celebratorily. The single highest-value question the campaign can answer autonomously at $0 is whether the H_011 anchor is DOSE-ROBUST or TOY-FRAGILE: the proxy env responds deterministically, immediately, and fully-observably to actions, while the real owner loop's measured utterance→input contingency is near-floor. Sweep the env's responsiveness (contingency dose) down toward owner-loop-like levels and measure where certified closure dies. If closure survives at realistic contingency, F8 is a substantive seat of aliveness and the campaign has a positive result independent of H_012B; if it collapses as soon as the env stops being a mechanical answer-machine, the anchor was a toy artifact, reading (ii) wins, and reframe C is effectively dead on both loops. H_012B stays LAUNCH-PENDING-CONSENT in parallel — it is a human gate, not autonomous work, so it costs nothing to keep queued.",

  "concrete_experiment": "H_013 closure-dose-response ($0, summer RTX 5070, autonomous). HYPOTHESIS: the 7B brain's certified block-closure (H_011 LV-C) degrades gracefully as the micro-tenant env's action-contingency is diluted, remaining ≥ the 0.60 anchor at a pre-registered dose d* calibrated (by the same sign-estimator run observationally in-world) to the real owner loop's measured contingency band (~0.42 sign_base_full); the falsifying outcome is collapse to the yoked floor at any dose materially above d*. DESIGN: reuse the H_011 Stage-A certified harness unchanged (LV-C block closure vs Watson marginal-matched yoked ghosts, LV-N null-env, LV-E echo guard) and add one knob: env responds to the agent's action with probability p ∈ {1.0, 0.75, 0.5, 0.25, 0.1} (otherwise acts on its own marginal), 5 episodes per dose, 7B Qwen2.5-Instruct nf4 brain frozen from H_011. ESTIMATOR: LV-C block closure per dose + tool/anima_v3 shift-null band and binom_sf gates (frozen AND-gate semantics from run-h011-py-1 convergence). PLANTS (≥1, running 3): P-DOSE — a scripted known-contingency brain at each p verifying the estimator tracks dose monotonically (certification gate, must pass before any verdict); P-DEAD — action-ignoring env must refuse at ALL doses; P-LIVE — full-dose replication of the H_011 anchor (0.76 ± band) as the positive control. PRE-REGISTERED GATES/VERDICTS: CD-1 monotonicity (closure non-increasing in dilution, Spearman over doses); CD-2 anchor-at-d* (closure ≥0.60 at the owner-calibrated dose) → verdict DOSE-ROBUST (F8 relocation is substantive; also yields a quantitative prediction for H_012B: expected closure at the owner loop's contingency); CD-2 fail with CD-1 pass → verdict TOY-FRAGILE (H_011 anchor is an artifact of mechanical responsiveness; reframe C presumed dead on both loops, campaign pivots to reframe D or accepts H_012B as sole residue); plant failure → INSTRUMENT-INVALID, no substrate verdict. This is genuinely $0, fully autonomous, reuses only certified primitives, and is the discriminating experiment between tension readings (i)/(ii) — which no amount of further owner-transcript analysis can be."
}
```
